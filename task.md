# 开发任务拆解文档（task.md）

**关联文档**：`prd.md`（v1.0-rc5）、`backend.md`（v0.1）
**版本**：v0.1（Draft）
**日期**：2026-08-18
**团队**：PM ×1、前端工程师 ×1（客户端）、后端工程师 ×1（服务端 + 用户后台前端 + 管理后台前端）
**工期**：MVP 1 个月（第 1～4 周，对应 PRD §8）

---

## 0. 使用说明

### 0.1 任务拆分原则

1. **从登录鉴权开始**：PRD §2.4 第 9 条「服务端所有接口都需要身份认证」是全局前置——不先完成鉴权，团队、端口、翻译、计量等一切业务接口都无法安全地对外。因此本拆解以 **M1 登录鉴权体系**为第一个开发模块，其余业务模块按依赖顺序依次展开。
2. **数据模型先行**：M0 一次性建好全部 Prisma 模型（backend §3），后续各模块直接复用，避免反复改表。
3. **先服务端、后后台页面、再客户端**：后端第 1～2 周集中产出可调接口，第 3 周开发两个后台页面，客户端由前端并行推进（消息稳定标识为 go/no-go 关卡）。
4. 每个任务给出 **需求来源、依赖、开发内容、验收要点**；验收要点直接引用 PRD AC 编号，可在实现后对照附录 A 转成测试用例。

### 0.2 编号规则

- **模块（M）**：`M0` 工程基础 → `M1` 登录鉴权 → `M2` 团队与配额 → `M3` 端口租约 → `M4` 翻译代理 → `M5` 用量计量 → `M6` 用户后台前端 → `M7` 管理后台前端 → `M8` 客户端 → `M9` 联调与发布。
- **任务（T）**：`T<模块>-<序号>`，如 `T1-03` 为 M1 的第 3 个任务。
- **优先级**：`P0`（必须有，阻塞后续）/ `P1`（应该有，可后置）。

### 0.3 状态约定

| 状态 | 含义 |
|------|------|
| 待办 | 未开始 |
| 进行中 | 开发中 |
| 待验收 | 开发完成，等 PM/QA 对照 AC 验收 |
| 已完成 | 验收通过 |

---

## 1. 任务总览

| 模块 | 名称 | 任务数 | 关联需求（PRD） | 责任方 | 建议周次 |
|------|------|-------|-----------------|--------|---------|
| M0 | 工程基础 | 5 | §3 数据模型、§12 可配置项 | 后端 | 第 1 周 |
| **M1** | **登录鉴权体系** | **6** | **P0-A-01 / P0-A-02 / P0-A-19 / P0-B-09（服务端）** | **后端** | **第 1 周** |
| M2 | 团队与配额管理 | 4 | P0-S-11 | 后端 | 第 1～2 周 |
| M3 | 端口租约 | 5 | P0-C-20 | 后端 | 第 2 周 |
| M4 | 翻译代理与引擎管理 | 8 | P0-S-12 / P0-T-07（服务端） | 后端 | 第 2～3 周 |
| M5 | 用量计量与配额裁决 | 4 | P0-B-10 / P0-S-11 | 后端 | 第 3 周 |
| M6 | 用户后台前端 | 6 | P0-A-19 / P0-B-09 / P0-B-10 / P1-B-16 | 后端 | 第 3 周 |
| M7 | 管理后台前端 | 4 | P0-A-19 / P0-S-11 / P0-S-12 / P1-S-17 | 后端 | 第 3 周 |
| M8 | 客户端 | 12 | P0-C-03 / P0-C-18 / P0-M-05 / P0-T-06 / P0-T-07 / P0-T-08 / P1-C-15 / P1-T-13 | 前端 | 第 1～4 周 |
| M9 | 联调、异常验证与发布 | 5 | §8 出口条件、附录 A | 全员 | 第 4 周 |

### 依赖关系（简化）

```
M0 工程基础
  └──▶ M1 登录鉴权体系 ──▶ M2 团队与配额 ──▶ M3 端口租约
                │                     └────▶ M4 翻译代理 ──▶ M5 用量计量
                │
                ├──▶ M6 用户后台前端（依赖 M1/M2/M3/M5 接口）
                ├──▶ M7 管理后台前端（依赖 M1/M2/M4/M5 接口）
                └──▶ M8 客户端（依赖 M1 服务端接口 + M3/M4 接口）
                          │
                          └──▶ M9 联调、异常验证与发布（依赖全部）
```

---

## 2. 任务明细

---

### M0 工程基础（前置）

> 现有工程 `matreko-server` 已定型（Express 4 + TS 5.7 严格模式 + Prisma 6 + SQLite + Zod 3 + Swagger），本模块在其上补齐依赖与骨架，不重建。

#### T0-01 依赖补齐与工程规范

- **优先级**：P0｜**需求来源**：backend §1
- **依赖**：无
- **开发内容**：
  - 新增依赖：`jsonwebtoken`（JWT）、`bcryptjs`（密码哈希）、`node-cron`（定时任务）；必要时 `axios`（翻译引擎 HTTP 调用，或直接使用内置 fetch）。
  - 统一错误码枚举与响应结构（`ApiResponse` 扩展，支持 `401/403/404/429` 与业务可区分错误码，如 `QUOTA_EXHAUSTED`、`TRANSLATION_SERVICE_UNAVAILABLE`、`LANGUAGE_UNSUPPORTED`）。
  - 清理示例代码（user/post），仅保留脚手架范式。
- **验收要点**：工程可 `npm run dev` 启动；错误码枚举齐全；Swagger UI 可访问。

#### T0-02 数据模型定义与迁移

- **优先级**：P0｜**需求来源**：PRD §2.6、backend §3.2
- **依赖**：T0-01
- **开发内容**：在 `prisma/schema.prisma` 中一次建齐 11 张表，并完成首次迁移与 seed 骨架：

  | 表 | 关键字段 / 约束 |
  |----|----------------|
  | `Team` | `createdAt` 不可改、`expiresAt` 秒级精确、`portQuota`、`translationQuota`（默认 1_500_000）、`translationUsed`（冗余计数）、`status` |
  | `AdminAccount` | `role`（PLATFORM/SUPERVISOR）、`email` unique、`passwordHash`、`teamId`、`status`、`failedLoginCount`、`lockedUntil`；PLATFORM 全系统唯一、SUPERVISOR 每团队唯一 |
  | `LicenseKey` | `teamId`、`codePrefix` + `codeHash`（不存明文）、`status`（UNUSED/ACTIVE/DISABLED）、`multiDeviceEnabled`（默认 false）、`deviceLimit`=5 |
  | `DeviceBinding` | `(keyId, fingerprintHash)` 唯一、`deviceLabel`、`boundAt` |
  | `ClientCredential` | `keyId`、`deviceFingerprintHash`、`clientId` unique、`refreshTokenHash` unique、`expiresAt`（滑动续期）、`lastRenewedAt` |
  | `PortLease` | `teamId`、`keyId`、`clientId`、`channelAccountKey`、`status`（HELD/RELEASED）、`acquiredAt`、`lastSeenAt`、`releasedAt` |
  | `TranslationKey` | `engine`、`name`、`keyEncrypted`（AES-256-GCM）、`status`（ACTIVE/EXHAUSTED/INVALID/DISABLED）、`quotaLimit`、`quotaUsed`、`lastFailureReason` |
  | `EngineLanguageSupport` | `(engine, languageCode)` 唯一、`status`（SUPPORTED/UNSUPPORTED）、`updatedAt` |
  | `TranslationUsageLog` | `teamId`、`keyId`、`engine`、`chars`、`success`（恒 true）、`createdAt`（计量唯一真相源） |
  | `AuditLog` | `actorType`、`actorId`、`action`、`detail`、`ip`、`createdAt` |
  | `RevokedToken` | `jti` unique、`reason`（RENEWED/REVOKED）、`revokeAt` |
  | `BackofficeSession` | `accountId`、`tokenHash` unique、`expiresAt`、`revokedAt` |
- **验收要点**：`prisma migrate dev` 成功；时间字段为 `DateTime`；「禁用不删除」语义由模型约束体现（无 delete 级联清理业务表）。

#### T0-03 环境变量与可配置项

- **优先级**：P0｜**需求来源**：backend §12
- **依赖**：T0-01
- **开发内容**：`src/config/env.ts` 扩展，读入并校验全部环境变量（含默认值），更新 `.env.example`：

  `ACCESS_TOKEN_TTL`(15min)、`REFRESH_TOKEN_TTL`(24h)、`TOKEN_RENEW_GRACE_MS`(60s)、`KEY_STATUS_CACHE_TTL_MS`(60s)、`RATE_LIMIT_TRANSLATE_CAP/RATE`(20/20s)、`RATE_LIMIT_OTHER_CAP/RATE`(50/50s)、`RATE_LIMIT_ACTIVATE_CAP/RATE`(5/1s)、`HEARTBEAT_INTERVAL`(2min)、`LEASE_TTL`(24h)、`LEASE_SCAN_INTERVAL`(1min)、`ENGINE_MAX_CHARS`(5000)、`CHUNK_SIZE`(4500)、`LANG_SYNC_CRON`(`0 4 * * *`)、`PLATFORM_EMAIL`、`PLATFORM_INITIAL_PASSWORD`、`API_KEY_ENC_KEY`、`LOGIN_LOCK_THRESHOLD`(10)、`LOGIN_LOCK_MS`(30min)、`BACKOFFICE_SESSION_TTL`(30min，后台会话有效期，滑动续期）。
- **验收要点**：启动时缺 `PLATFORM_EMAIL`/`PLATFORM_INITIAL_PASSWORD`/`API_KEY_ENC_KEY` 给出明确报错；所有参数可被 QA 调短以构造验收路径。

#### T0-04 基础中间件与进程内缓存

- **优先级**：P0｜**需求来源**：backend §4.5、§8.2
- **依赖**：T0-01
- **开发内容**：
  - `RateLimiter` 接口 + 内存令牌桶实现（单实例；未来可换 Redis 实现，接口预留）。
  - 通用 TTL 内存缓存工具（团队状态 / 密钥状态 / 引擎语种 / 配额缓存，支持主动失效）。
  - Zod 请求校验中间件、全局错误处理扩展（把业务错误码映射为 HTTP 状态码 + 响应体）。
- **验收要点**：超限返回 429 + `Retry-After` + `{ retryAfterMs }`；缓存支持按 key 主动失效。

#### T0-05 审计日志基础设施

- **优先级**：P0｜**需求来源**：backend §3.2 `AuditLog`、§10
- **依赖**：T0-02
- **开发内容**：`AuditLog` 写入工具函数（记录 actorType/actorId/action/detail/ip），后续所有安全事件统一调用：凭据篡改、指纹不匹配、团队/密钥禁用、配额调整、手动释放端口、后台登录锁定等。
- **验收要点**：工具函数可在事务内调用；`credential_tampered`、`fingerprint_mismatch` 等标准 action 枚举已定义。

---

### M1 登录鉴权体系（第一开发模块）

> **为什么从登录鉴权开始**：PRD §2.4 第 9 条要求服务端所有接口都需身份认证；两套凭据体系（客户端 / 后台）是 M2～M7 全部业务接口的前置依赖。本模块完成「三套认证入口 + 一套凭据运行时」：后台账号登录（主管/管理员）、客户端密钥激活、客户端凭据续期与鉴权、单客户端限流。
> **对应 PRD 第 1 周后端工作**：团队/密钥/硬件绑定/后台账号数据模型；身份认证与凭据自动续期；后台登录；单客户端限流。

#### T1-01 后台账号体系（登录 / 锁定 / 登出 / 改密 / 重置 / 禁用）

- **优先级**：P0｜**需求来源**：PRD P0-A-19（AC1～AC17）、backend §3.2、§4.6
- **依赖**：T0-02、T0-03、T0-05
- **开发内容**：
  - 部署初始化：服务启动时若不存在 `AdminAccount(role=PLATFORM)`，用 `PLATFORM_EMAIL` / `PLATFORM_INITIAL_PASSWORD` 创建（无新增入口，P0-A-19 AC1）。
  - 登录接口 `POST /api/{supervisor|platform}/auth/login`：
    - 邮箱 + 密码 → 校验锁定状态（`lockedUntil`）→ 校验密码（bcrypt cost=12）；
    - 失败统一提示「邮箱或密码错误」（防枚举，AC6）；
    - 连续失败 ≥10 次锁定 30 分钟（AC7）；锁定期内重试不延长锁定期（AC8）；成功即清零（AC9）；
    - 团队被禁用或到期 → 主管拒绝登录（AC12）；
    - 成功后签发 `BackofficeSession`（不透明随机串 32 字节，库中存哈希，**有效期 30 分钟**）；会话采用**滑动续期**：`requireBackofficeAuth` 中间件在每次 API 调用校验通过后刷新 `expiresAt = now + BACKOFFICE_SESSION_TTL`，**连续 30 分钟无任何 API 调用则凭据失效、需重新登录**（原「固定 8 小时、登录后不再刷新」的登录态已废弃，`.env` 与 `.env.example` 的 `BACKOFFICE_SESSION_TTL` 已由 28800000ms 改为 1800000ms）。
  - 登出 `POST /auth/logout`：`revokedAt` 置位，会话立即失效（AC4）。
  - 改密 `POST /auth/change-password`：密码规则校验（≥8 位且含字母与数字，AC10）；改密成功后**其余**已登录会话全部失效、当前会话保留（AC5）。
  - 管理员重置主管密码（管理后台）：该主管**全部**会话失效（AC13）；产品内无找回密码入口。
  - 后台账号禁用（不可删除，AC14）；平台管理员不可禁用（AC15）。
- **验收要点**：P0-A-19 AC1～AC17 全量覆盖；「锁定期不延长」与「成功清零」有单测；密码哈希不可还原、任何响应不含密码明文（AC16 常驻回归）；两套凭据互不通用（AC17 在 T1-04 联合验证）；**滑动续期验证**：连续调用 API 时 `expiresAt` 持续刷新（不提前失效），将 `expiresAt` 拨到过去后下一次调用应 401 并重登（30 分钟无操作即失效）。

#### T1-02 密钥生成与密钥管理（主管侧服务端接口）

- **优先级**：P0｜**需求来源**：PRD P0-B-09（AC1～AC13）、backend §5.2、§5.4
- **依赖**：T1-01（需登录态）
- **开发内容**：
  - 生成密钥 `POST /api/supervisor/licenses`：格式 `MTRK-` + 4 组 × 4 位 Base32；库中存 `codeHash`(SHA-256) + `codePrefix`(前 6 位)；**明文仅本次响应返回一次**；默认 `multiDeviceEnabled=false`、`status=UNUSED`（AC1）。
  - 密钥列表 `GET /api/supervisor/licenses`：状态 / 创建时间 / 多开开关 / 绑定设备列表与激活时间（AC2）。
  - 禁用密钥 `POST /api/supervisor/licenses/:id/disable`：不可删除、只能禁用（AC8）；提交前返回影响范围提示（AC7）；禁用后触发凭据失效（配合 T1-04 的撤销机制）。
  - 多开开关 `POST /api/supervisor/licenses/:id/multi-device`：开启上限 5 台（AC5）；**关闭时要求明确选择保留哪一台、下线哪些设备**，未选择不提交（AC9），下线设备走解绑流程（AC10）。
  - 设备解绑 `POST /api/supervisor/devices/:id/unbind`：删除 `DeviceBinding` + `ClientCredential` + jti 入撤销名单（AC4/AC11），无次数限制。
  - 并发保护：同一密钥的并发操作以服务端最终状态为准，后提交者返回「状态已变更」（AC12）。
- **验收要点**：P0-B-09 AC1～AC13；密钥明文只出现在生成响应；同密钥并发「开启/关闭多开」「解绑」互斥不脏写。

#### T1-03 客户端密钥激活与硬件绑定

- **优先级**：P0｜**需求来源**：PRD P0-A-01（AC1～AC16）、backend §5.1、§5.3
- **依赖**：T0-02、T0-03、T0-05
- **开发内容**：
  - `POST /api/client/activate`，入参 `{ code, fingerprint, deviceLabel }`：
    1. 查密钥（前缀 + 哈希比对）；不存在 →「密钥无效」（AC4）；
    2. 状态 = `UNUSED`/`ACTIVE` 且未禁用 → 否则「密钥已禁用」（AC5）；团队 `ACTIVE` 且未到期 → 否则按团队状态/到期提示（AC6/AC13）；
    3. **名额判定（事务内 `SELECT ... FOR UPDATE` 锁密钥行）**：未开多开且有绑定 →「该密钥已在其他设备激活」并展示已绑定设备标识与绑定时间（AC8）；开多开且绑定数 ≥5 →「已达设备数上限 5 台」（AC9）；有剩余名额 → 创建 `DeviceBinding`（AC1/AC3/AC12）；
    4. 签发 `clientId` + `ClientCredential(refresh)` + access token。
  - 指纹哈希：`SHA-256(fingerprint + keyId)` 存储；指纹变化一律按「新设备」走名额判定，**不存在独立失败路径**（AC12），激活成功后提示原设备仍占名额、需联系主管解绑（AC16）。
  - 幂等：同 `(keyId, fingerprint)` 重复激活返回「已激活」不产生第二条绑定（AC10）；并发抢最后名额由行锁 + `(keyId, fingerprintHash)` 唯一约束双保险（AC11）。
- **验收要点**：P0-A-01 AC1～AC16；并发激活同一密钥抢占最后 1 个名额时仅一台成功（用脚本并发压测验证）；团队到期/禁用场景返回可区分错误。

#### T1-04 客户端凭据体系：JWT 签发 / 无缝续期 / 鉴权链路 / 凭据隔离

- **优先级**：P0｜**需求来源**：PRD P0-A-02（AC1～AC12）、backend §4.1～§4.4
- **依赖**：T1-03（依赖激活产出的凭据）
- **开发内容**：
  - 凭据构成：access token（JWT HS256，含 `sub`=keyId、`client_id`、`device_fp_hash`、`token_type=client`、`jti`，有效期 15min）+ refresh token（256 位随机串，库中存 SHA-256，有效期 24h 滑动续期）。**双令牌设计动机**：PRD P0-A-02 只规定行为契约（续期无感知 AC2/AC12、禁用 ≤5min 断权 AC8），未规定凭据形态；单一短令牌会逼客服频繁重激活（违反 AC2），单一长签名令牌无法在禁用后 ≤5min 即时撤销（违反 AC8）。故拆分——access 为高频无状态短命令牌（性能 + 小泄露窗口），refresh 为低频、DB 托管、可即时作废/旋转的长命令牌（静默续期 + 即时吊销），以此同时满足"无感知续期"与"即时撤销"两个本质矛盾的需求（详见 backend.md §4.1.1）。
  - 无缝续期 `POST /api/client/auth/renew`：校验 refresh 哈希 → 校验密钥/团队状态与指纹 → **原子轮换**（旧 access jti 入 `RevokedToken(RENEWED)` 进入 60s 宽限期 → 签新 access → 删旧 refresh 插新记录）→ 返回新凭据（AC2/AC12）。
  - 登出 `POST /api/client/auth/logout`：撤销本机凭据。
  - **鉴权中间件**（所有 `/api/client/*` 请求）：
    1. JWT 签名与有效期校验 → 401，响应区分 `invalid`/`expired`（AC5/AC6）；
    2. `token_type=client` 校验，后台凭据调用 → 401（AC17）；
    3. jti 查撤销名单，命中且超宽限期 → 401；
    4. 密钥状态（缓存 TTL 60s）→ `DISABLED` → 403（AC8，禁用后 ≤5min 断权）；
    5. 团队状态（缓存）→ 禁用或到期 → 403；
    6. 硬件指纹比对（`X-Device-Fingerprint` 与签发时哈希一致）→ 不匹配 403 + 审计日志（AC9）；
    7. 单客户端限流（T1-05）。
  - 状态缓存主动失效：密钥禁用/解绑/团队禁用时写缓存失效，保证 ≤1min 生效。
  - 两套凭据隔离：`token_type` claim + 路由前缀双守卫（`/api/client/*` 强制 `client`，`/api/{supervisor,platform}/*` 强制 `backoffice`）。
- **验收要点**：P0-A-02 AC1～AC12；续期期间在途请求不 401（宽限期验证）；禁用密钥后 ≤60s 内 403；指纹不匹配返回 403 且 `fingerprint_mismatch` 审计日志落库；凭据轮换后旧 access 在宽限期后 401、旧 refresh 无法再用。

#### T1-05 单客户端限流

- **优先级**：P0｜**需求来源**：PRD P0-A-02 AC10/AC11、backend §4.5
- **依赖**：T0-04、T1-04（接入鉴权链路）
- **开发内容**：
  - 令牌桶按 `client_id` 维度计数（**绝不按团队或全局聚合**，AC11）。
  - 三组限流：翻译接口 20/20s；其余客户端接口 50/50s；激活接口 5/1s。
  - 后台登录接口按**账号**限 10 次/30min（与锁定策略联动）。
  - 超限返回 429 + `Retry-After` + `{ retryAfterMs }`（AC10）。
- **验收要点**：单客户端触发限流不影响同团队其他客户端（构造两客户端对比验证）；429 响应头与响应体字段齐全。

#### T1-06 客户端基础查询接口

- **优先级**：P0｜**需求来源**：PRD §4.1、P0-B-10 AC19、backend §9.1
- **依赖**：T1-04
- **开发内容**：
  - `GET /api/client/me`：当前密钥所属团队信息（团队名、状态、到期时刻）。
  - `GET /api/client/team/usage`：团队级汇总——端口「已占用/配额」、翻译「累计已用/配额总量」，与服务端记录同源（AC19）；**仅团队汇总，不含他人明细**。
  - `GET /api/client/dashboard`（首页仪表板，2026-08-24 增补）：一次性返回首页所需全部数据——服务端时间、团队名/状态/到期/剩余天数、顶部条团队端口 7/20、4 张统计卡片（我的账号、团队端口[本机/同事拆分]、翻译配额、翻译服务健康[OK/DEGRADED/OUTAGE]）、各渠道（Telegram/WhatsApp）的 5 项状态分布（在线/等待扫码/离线占端口/未启动），同时供侧栏「N/M 在线」角标使用。**「连续失败 N/10」为客户端熔断计数器（P0-T-08），客户端自维护，不入服务端**。
- **验收要点**：返回时间带 `Asia/Shanghai` 标注；客服视角看不到其他客服的密钥/账号/分项用量（权限边界回归）。

---

### M2 团队与配额管理（P0-S-11）

#### T2-01 团队创建（含主管账号原子创建）

- **优先级**：P0｜**需求来源**：PRD P0-S-11 AC1、P0-A-19 AC2、backend §4.6
- **依赖**：T1-01
- **开发内容**：`POST /api/platform/teams` 事务内同时创建 `Team` + `AdminAccount(role=SUPERVISOR)`；主管初始密码（随机生成）**仅响应返回一次**；配置到期时刻（日期+时分秒）、端口配额、翻译配额（默认 150 万字符）。
- **验收要点**：创建后团队为启用态；主管初始密码只在本次响应出现；任一子操作失败整体回滚。

#### T2-02 团队列表与查看

- **优先级**：P0｜**需求来源**：PRD P0-S-11 AC2、backend §8.4
- **依赖**：T2-01
- **开发内容**：`GET /api/platform/teams`：启用状态、创建时刻、到期时刻、端口占用、累计翻译用量与配额总量；时间统一 `Asia/Shanghai` 渲染。
- **验收要点**：时间口径统一；数据来自服务端记录。

#### T2-03 配额与到期时刻修改

- **优先级**：P0｜**需求来源**：PRD P0-S-11 AC3～AC5、AC10～AC13、P0-B-10 AC15～AC17
- **依赖**：T2-01
- **开发内容**：
  - `PUT /api/platform/teams/:id/quotas`（端口/翻译配额）与到期时刻修改接口；
  - 修改后写进程内缓存并主动失效 → **≤1min 生效**（AC3）；
  - 延长到期只改 `expiresAt`，创建时刻与累计用量不变（AC4）；改到未来恢复可用且用量延续（AC5）；
  - 改到过去 = 立即到期，提交前提示影响范围（AC11）；创建时刻不可修改（AC12）；配额非负整数校验（AC13）；
  - 端口配额下调低于当前占用：标记待客服选择，不强制关闭（联动 M3）。
- **验收要点**：配额变更 1min 内端侧生效；AC4/AC5/AC10～AC13 覆盖。

#### T2-04 团队禁用

- **优先级**：P0｜**需求来源**：PRD P0-S-11 AC8～AC9
- **依赖**：T2-01
- **开发内容**：`POST /api/platform/teams/:id/disable`：提交前返回影响范围（在线客服数、已启动账号数）；禁用后全团队凭据按 T1-04 链路拒绝；**无删除能力**。
- **验收要点**：禁用后客户端请求 403、主管登录被拒；数据保留可查。

---

### M3 端口租约（P0-C-20）

> 心跳续租协议（backend §6.1）：心跳 2min / TTL 24h / 扫描 1min / 撤销感知 ≤2min。

#### T3-01 端口申请（防超卖）

- **优先级**：P0｜**需求来源**：PRD P0-C-20 AC1/AC10、backend §6.1
- **依赖**：M1（鉴权）、T2-03（配额）
- **开发内容**：`POST /api/client/ports/acquire`：事务内校验团队 `HELD` 数 < 端口配额 → 建 `PortLease(status=HELD)`；并发防超卖 = `Team` 行锁 + 唯一约束 `(clientId, channelAccountKey, status=HELD)`。
- **验收要点**：并发申请至配额上限不多卖一台；端口用尽返回明确拒绝（P0-C-03 AC7 服务端侧）。

#### T3-02 心跳协议（在线证明 + 撤销感知）

- **优先级**：P0｜**需求来源**：PRD P0-C-20 AC2/AC8/AC12、backend §6.1
- **依赖**：T3-01
- **开发内容**：`POST /api/client/ports/heartbeat`，入参 `{ clientId, leaseIds: [...] }`：①刷新各 lease `lastSeenAt`；②比对服务端记录，返回 `revokedLeaseIds`（已回收/已撤销/配额下调需关闭的占用）。
- **验收要点**：撤销后最迟 2min 客户端感知（缩短 `HEARTBEAT_INTERVAL` 构造测试）；断线 24h 内恢复占用持续有效。

#### T3-03 端口释放与归零

- **优先级**：P0｜**需求来源**：PRD P0-C-20 AC3/AC4/AC7、backend §6.1
- **依赖**：T3-01
- **开发内容**：`POST /api/client/ports/release`（单个释放，置 `RELEASED`）、`POST /api/client/ports/reset`（本机占用归零，客户端启动/强杀重启时调用）。
- **验收要点**：释放后团队可用端口实时 +1；`reset` 后立即回收不等 24h（AC7）。

#### T3-04 租约回收扫描任务

- **优先级**：P0｜**需求来源**：PRD P0-C-20 AC5/AC6、backend §6.1
- **依赖**：T3-01
- **开发内容**：`node-cron` 按 `LEASE_SCAN_INTERVAL`(1min) 扫描 `lastSeenAt + LEASE_TTL < now` 且 `status=HELD` 的租约，置 `RELEASED`（不物理删除）。
- **验收要点**：缩短 `LEASE_TTL` 后跑通 AC5/AC6 自动回收路径。

#### T3-05 手动释放与配额下调联动

- **优先级**：P0｜**需求来源**：PRD P0-C-20 AC11/AC12、P0-S-11 AC6/AC7、backend §6.2
- **依赖**：T3-02、T3-04
- **开发内容**：后台手动释放接口（主管/管理员，`POST /api/{supervisor|platform}/ports/:leaseId/release`）；配额下调触发「待关闭清单」随心跳下发。
- **验收要点**：手动释放立即回收且客户端心跳感知；配额下调后客户端被禁止启动新账号直至占用满足新配额。

---

### M4 翻译代理与引擎管理（P0-S-12 / P0-T-07 服务端）

> **前置提醒**：OQ-48（引擎内多 Key 选路规则）为产品决策，本模块开工前须定稿（PRD §7 截止点=第 3 周）。默认按 backend §7.3 推荐方案实现（按剩余额度加权随机 + 最近失败避让）。

#### T4-01 翻译引擎适配层

- **优先级**：P0｜**需求来源**：backend §1、§7.1
- **依赖**：T0-03
- **状态**：待验收
- **开发内容**：`TranslationProvider` 接口 + Google / DeepL 两个 HTTP 适配实现；统一入参（text、sourceLang、targetLang）与出参（translatedText、detectedSourceLang）；连接超时与 5xx 归类。
- **验收要点**：两个引擎可独立完成一次翻译；超时/5xx/429 归入可重试错误分类。

#### T4-02 翻译接口（选路 + 调用 + 计量 + 错误码）

- **优先级**：P0｜**需求来源**：PRD P0-S-12 规则基线、P0-T-07 AC10/AC13、backend §7.1、§7.2
- **依赖**：T4-01、T5-01（计量写入）
- **状态**：待验收
- **开发内容**：`POST /api/client/translate`，入参 `{ text, sourceLang?, targetLang, direction }`：
  1. 配额检查（`Team.translationUsed < translationQuota`，达到返回 `QUOTA_EXHAUSTED`，P0-T-07 AC12）；
  2. 可用引擎集 A = 存在 `ACTIVE` Key 的引擎；语种支持集 S = `EngineLanguageSupport`；候选 C = A ∩ S；
  3. C 为空 → `LANGUAGE_UNSUPPORTED`（P0-T-08 AC3 终态失败）；仅单引擎支持 → 直接路由该引擎（P0-S-12 AC12）；
  4. C 非空 → 随机选引擎（NG-11，不配比）；引擎内按 T4-03 选 Key；
  5. 调用成功 → 插入 `TranslationUsageLog` 计量 + 更新冗余计数（P0-B-10 口径）；失败按 §7.5 处理（Key 失效自动换 Key 重试一次）。
  6. 响应 `{ translatedText, detectedSourceLang, engine, chars }`。
- **验收要点**：P0-S-12 AC3/AC7/AC8/AC9/AC12/AC15；P0-T-07 AC10/AC13 可区分错误码（`API_KEY_INVALID` / `QUOTA_EXHAUSTED` / `TRANSLATION_SERVICE_UNAVAILABLE` 等）。

#### T4-03 引擎内多 Key 选路（OQ-48）

- **优先级**：P0｜**需求来源**：PRD P0-S-12 第二层、OQ-48、backend §7.3
- **依赖**：T4-02
- **状态**：待验收
- **开发内容**：`ACTIVE` Key 按「剩余额度加权随机 + 最近 30min 失败率 >50% 暂避 5 分钟」选路；`quotaUsed >= quotaLimit` 自动置 `EXHAUSTED` 并跳过；同引擎全部 Key 不可用 → 引擎移出可用集。
- **验收要点**：同一引擎两个 Key，把其中一个额度打满后请求自动落到另一个、客服无感知（P0-S-12 AC15）；选路函数独立、便于产品定稿后切换规则。

#### T4-04 语种清单维护

- **优先级**：P0｜**需求来源**：PRD P0-S-12 AC12、backend §7.4
- **依赖**：T4-02
- **状态**：待验收
- **开发内容**：种子数据（Google 131 / DeepL 33，ISO 639-1）；`node-cron` 每日 04:00 UTC 同步官方语种清单；管理后台「语种支持」手动维护接口；运行时缓存 TTL 10min + 同步后主动失效。
- **验收要点**：新增语种无需发客户端新版本即生效；语种清单变更后选路正确。

#### T4-05 超长消息分片与合并

- **优先级**：P0｜**需求来源**：PRD P0-T-07 AC10、P0-B-10 AC12、backend §7.6
- **依赖**：T4-02
- **状态**：待验收
- **开发内容**：单条 ≤ `ENGINE_MAX_CHARS`(5000)；超长按句子边界分片（≤ `CHUNK_SIZE`=4500）逐片翻译后合并完整译文；任一片失败整条失败（不得返回截断译文 + 成功）。
- **验收要点**：超长消息返回完整译文；任一片失败整条失败且不计量（P0-B-10 AC12）；计量按原文总字符一次。

#### T4-06 翻译 Key 管理接口

- **优先级**：P0｜**需求来源**：PRD P0-S-12 AC1/AC2/AC4/AC6/AC13/AC14/AC16、backend §8.3
- **依赖**：T0-03（`API_KEY_ENC_KEY`）
- **状态**：待验收
- **开发内容**：`/api/platform/translation-keys` CRUD（无删除）：添加（**保存时连通性校验**，失败不保存、线上继续用旧配置，AC4）、停用/启用（AC14）、掩码展示（AC2/AC5）、失效自动标记与原因记录（AC15）；单 Key/单引擎风险提示（AC11）。
- **验收要点**：无效 Key 保存即报错；任何响应不出现明文或可反推片段（AC5 常驻回归）；额度耗尽前进度可见（AC16）。

#### T4-07 翻译 Key 用量查询（近 24h 统计）

- **优先级**：P1（AC13 为 P0，聚合统计可在 P1 简化）｜**需求来源**：PRD P0-S-12 AC13、backend §8.3
- **依赖**：T4-06、T5-03
- **状态**：待验收
- **开发内容**：按 Key 聚合：已消耗字符/额度上限、近 24h 调用与失败次数、最近使用时刻、最近失败原因。
- **验收要点**：Key 维度用量与团队维度用量互不混淆。

---

### M5 用量计量与配额裁决（P0-B-10）

#### T5-01 计量写入（唯一真相源）

- **优先级**：P0｜**需求来源**：PRD P0-B-10 计量口径、backend §8.1
- **依赖**：T0-02、T4-02
- **开发内容**：翻译接口返回成功即插 `TranslationUsageLog`（按该次原文字符数），并在**同一事务**内原子更新 `Team.translationUsed`、`TranslationKey.quotaUsed`。
- **验收要点**：失败/未调用不计量；重试多次成功只计一次；译文最终无处可用仍照常计量（P0-M-05 AC17）。

#### T5-02 配额耗尽判定与恢复

- **优先级**：P0｜**需求来源**：PRD P0-B-10 AC8/AC15/AC16/AC17、backend §8.2
- **依赖**：T5-01
- **开发内容**：翻译请求前检查配额；耗尽返回 `QUOTA_EXHAUSTED`；管理员调大恢复（用量不清零）；调到低于已用量 → 立即阻断并展示「已超出配额」；团队禁用后恢复 → 用量延续。
- **验收要点**：AC8/AC15/AC16/AC17 全量覆盖；配额变更 1min 内生效。

#### T5-03 近 24h 用量聚合任务

- **优先级**：P1｜**需求来源**：backend §8.3
- **依赖**：T5-01
- **开发内容**：`KeyDailyUsage` 缓存表，每小时聚合一次 + 实时计数兜底，避免每次查询扫全表。
- **验收要点**：近 24h 统计准确；聚合任务幂等可重跑。

#### T5-04 用量查询接口

- **优先级**：P0｜**需求来源**：PRD P0-B-10 AC1～AC5、P1-B-16
- **依赖**：T5-01
- **开发内容**：`GET /api/supervisor/accounts`（IM 账号）、`GET /api/supervisor/ports`（端口用量）、`GET /api/supervisor/translation-usage`（翻译用量）、按密钥用量分布（P1）；接口不可用时返回加载失败而非 0（AC13）。
- **验收要点**：数据实时、服务端记录为准；团队范围权限校验（AC14）。

---

### M6 用户后台前端（主管）

> Vue 3 + Vite + TS + Element Plus，构建产物由服务端静态托管 `/backoffice/supervisor`（backend §11）。

#### T6-01 工程初始化 + 登录页 + 会话管理

- **优先级**：P0｜**需求来源**：PRD P0-A-19、backend §11
- **依赖**：T1-01
- **开发内容**：Vue3 工程脚手架；登录页（统一错误提示、锁定剩余时间展示）；axios 拦截器（401 跳登录）；登出。
- **验收要点**：登录失败提示不区分邮箱/密码（AC6）；锁定期间展示剩余时间（AC7）。

#### T6-02 密钥管理页

- **优先级**：P0｜**需求来源**：PRD P0-B-09、backend §11
- **依赖**：T1-02
- **开发内容**：密钥列表（状态/创建时间/多开开关/设备列表）、生成（明文一次性展示）、禁用（含影响提示）、多开开关（关闭时选留设备弹窗）。
- **验收要点**：P0-B-09 AC1～AC3、AC7～AC9 的界面交互完整；并发变更展示「状态已变更」。

#### T6-03 设备管理与解绑

- **优先级**：P0｜**需求来源**：PRD P0-B-09 AC4/AC10/AC11
- **依赖**：T1-02、T6-02
- **开发内容**：设备列表与解绑入口；解绑/下线在线设备前的确认提示。
- **验收要点**：解绑交互明确提示影响（对应客服 5min 内下线）。

#### T6-04 IM 账号查看页

- **优先级**：P0｜**需求来源**：PRD P0-B-10 AC1
- **依赖**：T5-04
- **开发内容**：本团队所有密钥下已添加的渠道账号及渠道、状态、所属密钥；空态展示。
- **验收要点**：AC1、AC6 空态不报错。

#### T6-05 端口用量页（含手动释放）

- **优先级**：P0｜**需求来源**：PRD P0-B-10 AC2/AC3/AC7、P0-C-20 AC11
- **依赖**：T5-04、T3-05
- **开发内容**：配额/已占用/可用实时展示；超限提示；手动释放入口（提交前确认「该客服可能仍在使用」）。
- **验收要点**：AC2/AC3/AC7；手动释放后数字实时更新。

#### T6-06 翻译用量页 + 按密钥分布（P1）

- **优先级**：P0（页面）/ P1（按密钥分布）｜**需求来源**：PRD P0-B-10 AC4/AC8、P1-B-16
- **依赖**：T5-04
- **开发内容**：累计已用/配额总量/剩余；已耗尽展示阻断说明（AC8）；按密钥拆分端口与翻译用量，缺失行补 0，禁用密钥历史可查（P1）。
- **验收要点**：AC4/AC8；P1-B-16 AC1～AC3。

---

### M7 管理后台前端（平台管理员）

> 托管路径 `/backoffice/platform`。

#### T7-01 工程初始化 + 登录页 + 路由权限

- **优先级**：P0｜**需求来源**：PRD P0-A-19、backend §11
- **依赖**：T1-01
- **开发内容**：管理后台脚手架；登录页（同 M6 交互）；角色守卫（仅平台管理员可进）。
- **验收要点**：登录交互同 P0-A-19；团队主管凭据无法访问管理后台（AC17）。

#### T7-02 团队管理页

- **优先级**：P0｜**需求来源**：PRD P0-S-11
- **依赖**：T2-01～T2-04
- **开发内容**：团队列表（状态/创建/到期/端口占用/翻译用量）、创建团队（含主管初始密码一次展示）、配额与到期时刻编辑（时间精确到时分秒）、禁用（含影响提示）、手动释放端口。
- **验收要点**：P0-S-11 AC1～AC5、AC8～AC13 界面完整；时间统一 `Asia/Shanghai`。

#### T7-03 翻译引擎与 Key 管理页

- **优先级**：P0｜**需求来源**：PRD P0-S-12、backend §11
- **依赖**：T4-06、T4-07
- **开发内容**：引擎状态卡片（可用/不可用、原因可一眼区分「额度用尽」vs「Key 失效」）；Key 添加（含连通性校验失败展示）/停用/掩码/实时用量/近 24h 统计；语种支持维护；单 Key/单引擎风险提示。
- **验收要点**：P0-S-12 AC1/AC2/AC4/AC10/AC11/AC13/AC14/AC16 界面覆盖；AC5 掩码常驻回归。

#### T7-04 运行监控页（P1）

- **优先级**：P1｜**需求来源**：PRD P1-S-17
- **依赖**：T5-04
- **开发内容**：各团队在线账号数、端口占用、翻译成功率与失败原因分布；禁用/到期团队标注；配额耗尽高亮。
- **验收要点**：P1-S-17 AC1～AC3。

---

### M8 客户端（前端工程师）

> 客户端为独立工程（跨平台 Windows/Linux/macOS），通过 backend §9.1 接口清单与服务端对接。消息稳定标识为第 1 周 **go/no-go 关卡**。

#### T8-01 消息稳定标识可行性验证（go/no-go）

- **优先级**：P0（第 1 周最高优先，硬性未知）｜**需求来源**：PRD §6.5 R3、P0-M-05 消息标识条款、backend §9.2、§14 待决 4
- **依赖**：无（技术预研）
- **开发内容**：在两个渠道 Web 端上验证：从 DOM 中确定性提取消息标识（格式 `{channel}:{accountId}:{确定性摘要}`），跨页面重绘 / 滚动回退 / 重启 / 重新登录均稳定；给出确定性算法与最坏情况兜底策略。
- **验收要点**：同一条消息任意时刻任意次识别得到相同标识；**不通过则不推进第 2 周**（PRD §8 出口条件）。

#### T8-02 客户端骨架 + 硬件指纹生成

- **优先级**：P0｜**需求来源**：backend §14 待决 3、PRD §2.4 第 7 条
- **依赖**：T8-01 结论
- **开发内容**：跨平台工程骨架（Electron/Tauri 等，由技术选型验证确定）；硬件指纹生成方案（跨盘/网卡变化规则，服务端只比对哈希）；跨平台打包与防反编译加固方案验证（R4）。
- **验收要点**：指纹稳定生成；一般 IT 人员无法轻易反编译；三端可打包。

#### T8-03 密钥激活与自动登录（客户端侧）

- **优先级**：P0｜**需求来源**：PRD P0-A-01（客户端侧 AC1/AC2/AC4～AC16）
- **依赖**：T1-03、T8-02
- **开发内容**：激活界面（密钥输入、错误提示区分）、激活成功进入主界面并持久化；重启自动登录（凭据本地持久化，**无论隔多久**不再要求重输密钥，AC2）；团队到期/被禁用的强制退出与登录页跳转（AC7/AC13）；启动时服务端不可达不允许离线启动（AC14）；运行中服务端不可达降级不退出（AC15）。
- **验收要点**：AC2「隔多久都能自动登录」为验收重点；降级运行不关实例、翻译标记不可用。

#### T8-04 凭据自动续期与降级运行（客户端侧）

- **优先级**：P0｜**需求来源**：PRD P0-A-02（客户端侧 AC2/AC6/AC7/AC10/AC12）、backend §4.1
- **依赖**：T1-04、T8-03
- **开发内容**：access 剩余 ≤5min 时静默调 renew（对客服无感知，AC2）；401 `expired` 触发静默续期一次，失败才要求重新登录（AC6）；续期期间网络中断持续重试（AC7）；429 按 `retryAfterMs` 延后重试（AC10）；凭据轮换期间在途请求不失败（AC12）。
- **验收要点**：续期全程无 UI 打扰；降级运行语义正确（翻译不可用、账号可用、不关实例）。

#### T8-05 渠道账号环境配置（代理 / 指纹 / 数据目录）

- **优先级**：P0｜**需求来源**：PRD P0-C-18（AC1～AC20）
- **依赖**：T8-02
- **开发内容**：新增账号草稿流（未保存草稿置灰启动、放弃二次确认、已有草稿不再追加）；代理配置（SOCKS5/HTTP(S)）+ 测试代理（返回出口 IP）；指纹一键随机生成 + 与代理出口地一致性校验（错位告警、重新生成）；独立数据目录分配；配置下次启动生效提示；删除清理数据目录。
- **验收要点**：P0-C-18 AC1～AC20 覆盖；代理未生效（出口 IP = 本机 IP）明确告警；两账号相同代理+指纹保存时重复告警。

#### T8-06 账号启动 / 扫码 / 停止 + 端口申请与心跳（客户端侧）

- **优先级**：P0｜**需求来源**：PRD P0-C-03、P0-C-20（客户端侧）、P1-C-15
- **依赖**：T3-01～T3-03、T8-05
- **开发内容**：启动前向服务端申请端口，获批才建浏览器实例（AC2）；等待扫码 / 在线 / 离线 4 态管理（P1-C-15）；停止释放端口；进程强杀后重启上报归零（AC12）；2min 心跳上报 + 按 `revokedLeaseIds` 关实例置「未启动」（P0-C-20 AC8/AC12）；离线不区分原因（P1-C-15 AC5）。
- **验收要点**：P0-C-03 AC7～AC14；端口占用/释放与服务端实时一致；服务端不可达时心跳退避重试、不关实例。

#### T8-07 注入脚本（Telegram / WhatsApp 两个渠道）

- **优先级**：P0｜**需求来源**：PRD P0-M-05（AC1～AC20 中的注入部分）、backend §9.2
- **依赖**：T8-01、T8-06
- **开发内容**：两个渠道 Web 端 DOM 注入脚本：识别文本消息、提取原文；按稳定标识去重；译文注入（双语展示）；非文本消息不提取不翻译不入库（AC7）；页面结构变化时注入失效降级提示（AC14）；对端撤回同步移除译文并标记（AC16）。
- **验收要点**：AC7/AC8/AC11/AC14/AC16；重复识别不重复翻译不重复计费；注入失效不影响 Web 端收发。

#### T8-08 双向翻译与目标语言识别（客户端侧）

- **优先级**：P0｜**需求来源**：PRD P0-T-06、P0-T-07（客户端侧）、§2.5
- **依赖**：T8-07、T4-02
- **开发内容**：接收方向固定翻中文；发送方向按会话目标语言（取联系人最新消息语言，无历史默认英文）；源语言自动识别（发送方输入可能非中文）；跳过翻译判定（源=目标/纯 emoji/URL/数字）；可视区域按需翻译；发送路径「先翻后发」（译文替换输入框内容再触发发送，本地留原文+译文）。
- **验收要点**：P0-T-06 AC1～AC7；P0-T-07 AC1～AC4/AC8/AC9/AC11/AC14；目标语言切换不重译历史消息。

#### T8-09 翻译重试 / 熔断 / 并发上限（客户端侧）

- **优先级**：P0｜**需求来源**：PRD P0-T-08、P1-T-13、backend §7.5
- **依赖**：T8-08
- **开发内容**：自动重试预算 40s（指数退避+抖动 `0.5→1→2→4→8→8→8s`，最多 7 次）；单条消息至多一个在途请求；客户端级并发上限 20（跨账号共享、排队）；连续 10 次失败跨账号累计熔断（成功即清零）；熔断无自动恢复、手动探测；失败消息标记 + 手动重试入口（P1-T-13）；429 按 Retry-After 延后。
- **验收要点**：P0-T-08 AC1～AC10、P1-T-13 AC1～AC4；40s 总时长不可突破；快速滚动不形成洪峰。

#### T8-10 本地消息库与去重

- **优先级**：P0｜**需求来源**：PRD P0-M-05 AC6/AC8/AC10/AC15/AC18、backend §3.3 同步预留字段契约
- **依赖**：T8-07
- **开发内容**：本地消息库按 backend §3.3-A 字段集建模（业务字段必写 + 同步预留字段只写默认值）；按稳定标识去重；超长消息完整入库不截断（AC10）；写入失败明确提示且不阻断 Web 端（AC15）；重启后终态可达（AC18）。
- **验收要点**：本地库字段与同步契约一致（零迁移未来）；同消息不重复翻译/计量；`pending/translating` 时强杀重启后有限时间内进入终态。

#### T8-11 发送路径对齐与原文回显

- **优先级**：P0｜**需求来源**：PRD P0-M-05 AC5/AC13/AC19/AC20
- **依赖**：T8-08、T8-10
- **开发内容**：发送时译文替换输入框触发发送（AC5）；翻译失败/配额耗尽弹窗由客服决定是否发原文，取消时保留原始输入（AC13）；发出的消息回显时把本地记录与 DOM 绑定为同一条并注入**原始输入**作对照（发送方向注入原文、接收方向注入译文，AC19），**不得再次调翻译接口**；对齐失败标注「未对齐」不重翻（AC20）。
- **验收要点**：AC13/AC19/AC20；对齐失败不产生第二条本地记录、不重复计费。

#### T8-12 三端打包与防反编译加固

- **优先级**：P0（第 4 周）｜**需求来源**：PRD §4.3、§6.2、R4、§8 第 4 周
- **依赖**：T8-02 方案验证、M8 各模块
- **开发内容**：Windows / Linux / macOS 三端发布包；按已验证加固方案打包（达到一般 IT 人员无法轻易反编译）。
- **验收要点**：三端安装运行通过；加固达标。

---

### M9 联调、异常验证与发布（第 4 周）

#### T9-01 端到端闭环联调

- **优先级**：P0｜**需求来源**：PRD §8 第 4 周出口条件
- **依赖**：M1～M8
- **开发内容**：跑通完整闭环——管理员创建团队 + 主管账号 → 主管登录生成密钥 → 客服激活 → 启动账号扫码登录 → 收发并双向翻译 → 查看端口与翻译用量 → 禁用密钥 5min 内下线。
- **验收要点**：PRD §8 第 4 周出口条件全项满足。

#### T9-02 故障注入与异常路径验证

- **优先级**：P0｜**需求来源**：PRD 附录 A（全部维度）
- **依赖**：M1～M8
- **开发内容**：按附录 A 逐维度注入故障（网络 / 服务端 / 认证 / 激活 / 账号与端口 / 端口回收 / 环境隔离 / 注入 / 消息类型 / 翻译方向 / 引擎与 Key / 熔断 / 配额 / 本地环境 / 计量口径 / 并发 / 权限），用缩短配置参数构造「24h 超时」「5min 感知」等验收路径。
- **验收要点**：附录 A 各维度全部通过；每条失败路径有对应 AC 断言。

#### T9-03 权限与安全回归

- **优先级**：P0｜**需求来源**：PRD P0-A-19 AC16/AC17、P0-S-12 AC5、§4.1
- **依赖**：M1～M8
- **开发内容**：密码/API Key 明文泄漏扫描（全接口响应常驻回归）；两套凭据串用拦截；客服越权与主管跨团队访问拦截；客户端团队配额展示不含他人明细。
- **验收要点**：安全回归用例全绿。

#### T9-04 压测与参数确认

- **优先级**：P1｜**需求来源**：PRD §8 第 4 周、backend §14 待决 2
- **依赖**：M4、M5
- **开发内容**：翻译并发 / 限流阈值 / 端口回收基础压测；与产品确认限流阈值、翻译并发上限、重试次数与退避曲线；**确认不得突破 PRD 承诺上限**（重试 ≤40s、单客户端限流不波及同事）。
- **验收要点**：压测结论落回配置项；承诺上限未被突破。

#### T9-05 发布检查与部署文档

- **优先级**：P0｜**需求来源**：backend §10（TLS）、§12（配置）、PRD §8
- **依赖**：T9-01～T9-04
- **开发内容**：部署文档（环境变量、Nginx TLS、静态托管路径、SQLite 备份）；发布检查清单（初始化管理员、seed 语种清单、加密密钥妥善保管）。
- **验收要点**：按文档可完成全新环境部署。

---

## 3. 里程碑与出口条件（对应 PRD §8）

| 里程碑 | 涉及模块 | 出口条件 |
|--------|---------|---------|
| **M1.1 第 1 周** | M0、M1、M2（部分） | 一台机器上能通过浏览器实例、经指定代理登录任一渠道 Web 端并成功读到一条消息、重启后仍识别为同一条；客户端能用密钥激活、主管能登录用户后台。（消息稳定标识不通过不推进第 2 周） |
| **M1.2 第 2 周** | M3、M4（部分） | 能配置并启动两个不同代理/指纹的账号、扫码登录、完成一轮收发，实例互不共享登录态；强杀客户端重启后端口被服务端立即回收；缩短配置下跑通超时回收路径。 |
| **M1.3 第 3 周** | M4、M5、M6、M7 | 收发消息自动翻译成功；注入超时/429/5xx/Key 失效四类故障后按 P0-T-07/P0-T-08 表现；同引擎两个 Key 打满一个后自动落到另一个、客服无感知。 |
| **M1.4 第 4 周** | M8、M9 | 完整闭环跑通；附录 A 全部通过；三端打包加固完成；压测参数确认且不突破承诺上限。 |

---

## 4. 依赖与风险

| # | 风险 / 依赖 | 说明 | 处理 |
|---|------------|------|------|
| 1 | **消息稳定标识**（T8-01）为 go/no-go 关卡 | 不成立则去重与防重复计费失去依据 | 第 1 周最高优先验证；不通过不推进第 2 周 |
| 2 | **后端 1 人承担服务端 + 两个后台前端** | 排期最薄一环，压力集中在第 3 周 | 后台页面「先够用、后好看」，P0 数据优先准确可见 |
| 3 | **OQ-48 引擎内多 Key 选路**未定稿 | 阻塞 M4 翻译代理定稿 | 产品在第 3 周开工前给结论；默认按 backend §7.3 推荐实现 |
| 4 | **客户端硬件指纹方案**未定 | 阻塞 T1-03 联调 | 前端第 1 周给出方案；服务端只比对哈希，接口已预留 |
| 5 | 限流/并发/退避参数需压测确认 | 第 4 周定稿 | 不得突破 PRD 承诺上限（40s、单客户端限流不波及同事） |
| 6 | 翻译引擎官方配额/费率需核对 | Google/DeepL 计费维度 | 后端第 2 周核对，影响 T4-06 Key 额度设计 |

---

## 5. 待决事项跟踪

| # | 事项 | 责任方 | 截止点 | 状态 |
|---|------|--------|--------|------|
| 1 | OQ-48 引擎内多 Key 选路规则（推荐：剩余额度加权随机 + 最近失败避让） | 产品 | M4 开工前（第 3 周） | 待定 |
| 2 | 限流阈值 / 翻译并发 / 退避参数压测确认 | 后端 + 产品 | 第 4 周 | 待定 |
| 3 | 客户端硬件指纹生成方案 | 前端 | 第 1 周 | 待定 |
| 4 | 消息稳定标识确定性算法（服务端固定格式契约，客户端实现） | 前端 + 后端 | 第 1 周（go/no-go） | 待定 |
| 5 | 翻译引擎官方配额 / 费率核对 | 后端 | 第 2 周 | 待定 |

---

> 本任务拆解与 backend.md 同步演进：backend.md 有变更时，需同步更新本文件的接口清单、依赖与验收要点。
