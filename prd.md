# 产品需求文档（PRD）：跨语言客服会话管理平台 v1.0

**作者**：imcstmi
**日期**：2026-08-16
**版本**：v1.0-rc5（Draft）
**依据**：`docs/releases/v1.0/brief.md`
**干系人**：产品经理 ×1、前端工程师 ×1、后端工程师 ×1（见 §4.3）
**变更记录**：见附录 B

> **本文档的写法约定**
> - 所有验收条件统一写成「**当** 某个前置条件/触发动作，**则** 系统产生某个可观察结果」，可直接转成测试用例。
> - 标注 `【派生】` 的结论来自底稿已定决策的直接推论；其余内容均直接对应底稿原文。

---

## 1. 摘要（Executive Summary）

面向做跨语言客服的团队，提供一套「客户端 + 用户后台 + 管理后台」的会话管理平台：客服在客户端为每个 IM 账号启动一个浏览器实例，登录 Telegram / WhatsApp 官方 Web 端，**在官方 Web 端原有的使用方式上，由客户端注入译文**实现收发消息自动翻译；主管通过密钥控制客服接入、管理设备绑定、查看端口与翻译用量；平台管理员管理团队、监控运行状态、集中管理翻译 API Key。

v1.0（MVP）覆盖 Telegram 与 WhatsApp 两个渠道、Google 与 DeepL 两个翻译引擎，消息仅存客户端本地，要求 **1 个月内完成开发**。

---

## 2. 背景与上下文（Background & Context）

### 2.1 问题空间

跨语言客服团队当前面对三个叠加的问题：

- **渠道分散**：一个客服要同时盯多个 IM 渠道、多个账号，需要在多个原生客户端之间来回切换。
- **语言障碍**：客服不懂客户语言，依赖人工把消息复制到翻译工具再粘回来，收发两个方向都要做一次，效率与准确率都不可控。
- **管控缺失**：主管无法统一发放/回收客服的接入权限，也看不到 IM 端口和翻译资源的用量，成本与合规都不可见。

### 2.2 核心概念定义（全文统一口径）

| 概念 | 定义 |
|------|------|
| **渠道（Channel）** | Telegram / WhatsApp 等 IM 平台 |
| **渠道账号（Channel Account）** | 客服在某个渠道上登录的账号；一个客服可登录 N 个 |
| **会话（Conversation）** | 一个渠道账号 ↔ 一个联系人 的消息序列 |
| **消息（Message）** | 含 原文 / 译文 / 源语言 / 目标语言 / 翻译状态 |
| **密钥（License Key）** | 由团队主管生成并发放给客服，用于激活并登录客户端；**有效期等于所属团队的有效期**。**一个密钥即代表一个客服**——系统中不存在独立的客服账号实体，所有「按客服」的统计与权限均以密钥为单位 |
| **端口（Port）** | 客户端上每个**已启动**的 IM 账号所占据的空间。一个已启动账号 = 一个浏览器实例 = 一个端口；**账号离线不释放端口，只有停止账号才释放** |
| **后台账号** | 主管与平台管理员用于登录用户后台 / 管理后台的账号，采用**邮箱 + 密码**（邮箱仅作账号标识，**不做邮箱验证**），与客户端的密钥是**两套独立体系**。**平台管理员全系统只有 1 个；每个团队只有 1 个主管账号**（见 P0-A-19） |

### 2.3 为什么是现在

**已有意向客户**，但没有外部时间约束（无客户承诺的截止日期、无合规期限、无竞品窗口）。1 个月的工期是内部设定的目标，不是外部硬约束——这一点在 §8 的范围风险处理中会用到。

### 2.4 已定的技术前提

1. 渠道接入在**客户端**，翻译接入在**服务端**。
2. 渠道接入形态：**客户端启动浏览器实例访问官方 Web 端，客服自行扫码登录**。
3. 客户端**每收一条消息调一次翻译接口**。
4. **收到的和发出的消息都翻译**。
5. 消息**只存客户端本地**；v1.0 不做任何同步，但**数据模型须为未来的同步留出空间**——将来上同步时不必迁移或丢弃已有历史消息（预留哪些字段由后端设计文档决定）。
6. 翻译引擎 API Key **只存在于管理后台**，不下发到客户端和用户后台。
7. 客户端采用跨平台语言，可在 Windows / Linux / macOS 发布；打包需做到**一般 IT 人员无法轻易反编译**。
8. 密钥激活需**硬件绑定**，默认一密钥一设备，可选多开（密钥级开关，上限 5 台）。
9. 服务端所有接口都需要身份认证；凭据**自动续期、客服全程无感知**；须有**单客户端维度**的限流，防止单个客服拖垮服务端或波及同事（有效期与阈值由后端设计文档决定）。

### 2.5 语言口径（全文统一定义）

翻译有**两个方向**，两端的语言分别由不同规则确定：

| 方向 | 源语言 | 目标语言 |
|------|--------|---------|
| **接收**（联系人 → 客服） | 联系人消息的**识别语言** | **中文**（固定，不提供修改） |
| **发送**（客服 → 联系人） | 客服输入内容的**识别语言**（通常是中文，也可能是英文或其他语言） | 该会话的目标语言（见 P0-T-06） |

- **两个方向的源语言都靠自动识别**——客服大多输入中文，但偶尔会直接敲英文或其他语言，不能假定发送方源语言恒为中文。
- **接收方向的目标语言固定为中文**，不提供团队级或客服级的配置项。
- 「会话目标语言」只作用于**发送方向**；接收方向的目标语言恒为中文。
- 译文语言与界面语言是两回事：界面不做多语言（NG-17）。

### 2.6 时间与配额口径（全文统一定义）

本产品所有与时间相关的判定**一律精确到秒**，不存在「按天」的模糊判定。团队只有两个时间字段，**没有续费概念**——延长有效期就是直接修改到期时刻。

| 字段 | 定义 | 精度 | 可修改性 |
|------|------|------|---------|
| **创建时刻**（`created_at`） | 团队被创建的那一刻，由服务端写入 | 精确到秒 | **不可修改** |
| **到期时刻**（`expires_at`） | 团队有效期的终止时刻 | 精确到秒（配置项含日期 + 时分秒） | **管理员可随时修改**，改大即延长、改小即提前到期 |

**翻译配额是累计总量，不是周期额度**

- 翻译配额为**团队级的累计上限**，单位字符，**默认 150 万字符**。
- 用量自团队创建起**一直累加，不按任何周期重置、不清零**；用满即阻断翻译。
- 需要继续使用时，由**管理员直接调大配额值**——没有「新周期自动恢复」这回事。
- 因此**用量历史不按周期区分**：任一时刻只有「累计已用字符数 / 配额总量」这一组数字。

**时区**：存储统一使用 UTC 时间戳，到期判定在 UTC 时间轴上进行；展示统一固定为 `Asia/Shanghai`（UTC+8），全系统一致、不提供任何时区配置项，展示时带时区标识。

---

## 3. 目标与成功指标（Objectives & Success Metrics）

### 3.1 目标（Goals）

1. **G1 聚合**：客服在一个客户端内管理多渠道、多账号，无需安装和切换多个原生客户端。
2. **G2 自动翻译**：收发双向消息自动翻译，客服无需手工复制粘贴到外部翻译工具。
3. **G3 可管控**：主管可通过密钥控制谁能接入、管理设备绑定，并看到端口用量与翻译用量。
4. **G4 资源集中**：翻译引擎凭据集中在管理后台，客户端与用户后台不接触 API Key。

### 3.2 非目标（Non-Goals）—— 明确不做

| # | 非目标 | 依据 |
|---|--------|------|
| NG-1 | **任何云端同步**（消息、账号环境配置） | 底稿明确不做消息云端同步；账号配置同理只存本地。仅预留同步字段，不实现任何上行链路 |
| NG-2 | **会话分配 / 排队** | 底稿明确不做。不做自动派单、会话池、客服负载均衡 |
| NG-3 | **机器人自动回复** | 底稿明确不做。不做关键词自动回复、AI 代答 |
| NG-4 | **数据报表** | 底稿明确不做。用量页仅展示当前数值，不做趋势图、多维分析、报表导出 |
| NG-5 | **Telegram / WhatsApp 之外的渠道** | MVP 范围只做这两个渠道 |
| NG-6 | **Google / DeepL 之外的翻译引擎** | MVP 范围只做这两个引擎 |
| NG-7 | **客户或团队自带翻译 API Key（BYOK）** | 【派生】API Key 只在管理后台 |
| NG-8 | **跨设备自由漫游** | 【派生】密钥硬件绑定；换机须由主管执行解绑 |
| NG-9 | **移动端（iOS / Android）客户端** | 【派生】目标平台为 Windows / Linux / macOS |
| NG-10 | **人工译文校对 / 术语库 / 翻译记忆库** | 底稿未纳入 MVP |
| NG-11 | **翻译引擎的流量配比与优先级配置** | 引擎自动选择（可用引擎中随机），不做配比和优先级 |
| NG-12 | **非文本消息与渠道特有类型的翻译和建模**（图片 / 语音 / 文件 / 贴纸 / 位置 / 模板消息 / 交互按钮 / 富文本） | 消息模型取各渠道最小公共集。此类消息由渠道 Web 端原生收发与渲染、照常可用，本产品**不翻译、不注入译文、不写入本地消息库**，也不做任何限制 |
| NG-13 | **自建会话列表与消息列表（含跨账号聚合）** | 注入式模型：会话列表、消息渲染、排序、历史加载均由渠道官方 Web 端负责，本产品不重新实现，也不做跨账号聚合视图 |
| NG-14 | **原文/译文显示模式切换** | 本期消息区固定双语展示（原文 + 译文） |
| NG-15 | **接收方向目标语言的可配置** | 固定中文（§2.5），不提供团队级或客服级设置 |
| NG-16 | **渠道掉线原因的区分与自动恢复** | 不与各渠道的掉线机制做深度耦合，异常统一归入「离线」，由客服手动重启账号 |
| NG-17 | **界面多语言（i18n）** | 不需要 |
| NG-18 | **离线使用能力** | 服务端不可达时客户端不允许启动；已登录的客户端**降级运行**（渠道收发照常、翻译不可用，见 P0-A-01 AC15），但不提供任何离线翻译能力 |
| NG-19 | **团队 / 密钥 / 后台账号的物理删除** | 只能「禁用」，禁用后相关数据保留不删除 |
| NG-20 | **计费相关的一切**：续费流程（含到期提醒、自助续期、续费记录）、超额计费、配额的周期性重置 | 本版不做。延长有效期即管理员直接改到期时刻；配额是累计总量，用满由管理员直接调大 |
| NG-21 | **代理资源的提供与托管（代理池、自动分配、按量计费）** | 只支持填入用户**自备**的代理，不销售、不托管、不做代理池调度 |
| NG-22 | **展示时区的配置能力** | 全系统固定 `Asia/Shanghai`（UTC+8） |
| NG-23 | **数据合规专项（出境审查 / 留存期限 / GDPR）** | 当前无此类要求 |
| NG-24 | **后台账号的邮箱验证与找回密码** | 邮箱仅作账号标识不做验证；主管忘密码由管理员重置，管理员忘密码由部署侧重置（P0-A-19 AC13） |

### 3.3 成功指标（Success Metrics）

| 指标 | 口径定义（可直接埋点） | 当前值 | 目标值 |
|------|----------------------|--------|--------|
| 翻译成功率 | 终态为 `succeeded` 的翻译任务数 ÷ 发起的翻译任务总数（含重试后成功） | 未知（新产品） | **≥ 99%** |
| 首次翻译成功率 | 无需重试即成功的翻译任务数 ÷ 翻译任务总数 | 未知（新产品） | **≥ 95%** |
| 接收消息端到端时延 | 注入脚本识别到新消息 → 译文注入到页面 的 P95 耗时（**仅统计首次即成功的翻译**，重试尾部不计入） | 未知（新产品） | **≤ 2s** |
| 发送消息端到端时延 | 客服点击发送 → **译文写入输入框并触发 Web 端发送** 的 P95 耗时（投递到渠道由 Web 端负责，客户端不可观测，不纳入口径） | 未知（新产品） | **≤ 1s** |
| 激活成功率 | 激活成功次数 ÷ 激活尝试次数（按密钥去重） | 未知（新产品） | **≥ 99%** |

**明确不作为产品指标**（底稿已判定，此处记录以防后续被重新引入考核）：

- **渠道账号在线率**：主要受用户自身网络环境与账号状态影响，不由本产品决定。
- **端口利用率**：由用户自行决定买多少、用多少，不由本产品决定。

> 指标口径的一致性约束（可作为测试断言）：跳过翻译的消息**不计入**翻译任务总数的分母；失败的翻译请求**不计入**翻译用量（口径见 P0-B-10）。

---

## 4. 目标用户、角色与团队

### 4.1 角色与权限边界

| 角色 | 使用的产品面 | 身份载体 | 数据可见范围 |
|------|------------|---------|------------|
| **客服人员** | 客户端 | 密钥（一密钥 = 一客服） | **只能查看自己的数据**，外加一项例外见下 |
| **团队主管** | 用户后台 | 后台账号（邮箱+密码，一团队一个） | **可查看本团队所有密钥的数据** |
| **平台管理员** | 管理后台 | 后台账号（邮箱+密码，全系统一个） | **可查看所有数据**；唯一可配置 API Key 的角色 |

**客服可见范围的唯一例外：团队端口与翻译配额的余量**

客服在客户端可以看到**团队级的两个汇总数字**——端口「已占用 / 配额」与翻译「累计已用 / 配额总量」。这不违反「只能查看自己的数据」，因为：

- 这两项都是**团队共用的消耗性资源**，同事启动账号会占掉端口、同事翻译会消耗同一份配额。客服看不见余量，就只能等到端口用尽、配额耗尽的那一刻才知道，而那时已经在停工了。
- 仅限**团队级汇总**：**不得**让客服看到其他客服的密钥、账号、设备或分项用量——那部分只有主管可见。

具体展示要求见 P0-B-10 AC19。

### 4.2 预期规模

| 维度 | 预期值 |
|------|--------|
| 团队数 | 100 个 |
| 客服数（= 密钥数） | 500 |
| 单人并发账号数 | 10 个 |
| 单客户端浏览器实例上限 | **不设限**（由客户端机器自身承载能力决定） |
| 客户端最低硬件配置 | 按可同时运行 5 个端口计 |
| 单团队翻译配额 | 累计 150 万字符（默认值，用满不自动重置） |

> 底稿中「日均 1000 万条消息」为非正式估算，**不作为容量设计依据**；容量以上表为准。

**v1.0 的实际投放规模**：上表是**设计目标规模**。v1.0 发布后只做**小规模内测——2 个团队、每团队约 5 人**。因此本版不为 100 团队 / 500 客服的量级做任何提前优化（连接数、分库分表、缓存层一律不做）；但**配额、计量、权限边界这三处的口径必须按设计目标规模写对**，因为它们是数据口径而非性能优化，事后改会污染历史数据。

### 4.3 团队分工与工期

| 角色 | 人数 | 职责 |
|------|------|------|
| 产品经理（Product Manager） | 1 | 业务决策、产品设计定义 |
| 前端工程师（Front-end Engineer） | 1 | **客户端**的设计开发 |
| 后端工程师（Backend Engineer） | 1 | **服务端 + 用户后台前端 + 管理后台前端**的设计开发 |

**工期要求：MVP 在 1 个月内开发完成。**

---

## 5. 用户故事与需求（User Stories & Requirements）

> 每条需求含**正常路径**与**异常路径**两组验收条件，异常路径是本章重点。
> 编号规则：`优先级-领域-序号`。领域：A=激活/认证，C=渠道与端口，M=消息，T=翻译，B=用户后台，S=管理后台。
> 序号按**签发顺序**分配，与阅读顺序无关；`P0-C-04`、`P1-M-14` 已作废且不复用。
> **本 PRD 仅描述 v1.0 范围**，不包含未来版本的需求。

### 5.1 P0 — 必须有（Must Have）

---

#### P0-A-01 密钥激活客户端（硬件绑定 + 多开）

**用户故事**：作为客服，我想用主管发给我的密钥激活客户端，以便开始接待客户。

**规则基线**：多开开关为**密钥级**；关闭多开时一密钥一设备；开启多开时设备数上限 **5 台**。密钥有效期 = 所属团队有效期。

**正常路径**
- AC1 当客服在未激活的客户端输入一个有效且未达设备上限的密钥并提交，则服务端将该密钥与本机硬件指纹绑定，客户端进入已激活状态并展示所属团队信息。
- AC2 当密钥激活成功后客服重启客户端，则客户端自动登录、不再要求重新输入密钥——**无论距上次使用过了多久**（关机过周末、长假后开机同样成立）。只要该密钥未被解绑、未被禁用、团队未到期，客服就不该再看到密钥输入框。服务端凭据的有效期是**内部机制**，**不得外溢成「隔一阵就要重输密钥」的用户体验**（实现方式见后端设计文档）。
- AC3 当密钥开启多开且已绑定设备数 < 5，则新设备可激活成功，绑定记录数 +1。

**异常路径**
- AC4 当输入的密钥在服务端不存在，则激活失败，提示「密钥无效」，且不写入任何本地激活状态。
- AC5 当输入的密钥已被主管禁用，则激活失败，提示「密钥已禁用，请联系主管」。
- AC6 当密钥所属团队已过**到期时刻**（精确到秒），则激活失败，提示团队已到期及具体到期时刻。
- AC7 当已激活的客户端在运行中检测到已过团队到期时刻，则弹窗提示后**强制退出登录并跳转至登录页**，不再加载任何会话。
- AC8 当密钥未开启多开且已绑定另一台设备，则激活失败，提示「该密钥已在其他设备激活」，并展示已绑定设备的可识别标识与绑定时间。
- AC9 当密钥已开启多开且已绑定设备数已达 5，则激活失败，提示「已达设备数上限 5 台，请联系主管解绑」。
- AC10 当激活请求发出后网络中断或服务端超时，则客户端展示可重试的失败态，且不得进入「已激活」状态；重试成功后同一密钥不得产生第二条绑定记录。
- AC11 当同一密钥在两台设备上并发提交激活且剩余名额为 1，则最终有且只有一台设备绑定成功，另一台收到 AC8/AC9 的失败提示。
- AC12 当已激活设备的硬件指纹发生变化（更换硬盘/网卡、迁移虚拟机），则**该设备一律按「一台新设备」处理**，走与 AC1 完全相同的名额判定：**仍有名额则激活成功**并新增一条绑定记录，**没有名额才失败**并按 AC8 / AC9 给出对应提示。**不存在「指纹不匹配」这一条独立的失败路径**——那种提示既不告诉客服该做什么，也让主管无从判断是换机还是盗用。
- AC16 当指纹变化导致按新设备激活成功，则**原设备的绑定记录依然存在、依然占用一个名额**；界面须明确告知这一点并提示「原设备不再使用请联系主管解绑」（解绑流程见 P0-B-09 AC4），否则名额会被废弃设备长期白占。
- AC13 当客服所属团队已被平台管理员禁用，则客户端校验失败并提示团队不可用，按 AC7 的方式退出到登录页。
- AC14 当客户端**启动时**无法连接服务端，则**不允许离线启动**，展示服务不可用提示与重试入口。
- AC15 当客户端**运行中**服务端变为不可达，则**不得退出登录、不得关闭任何已启动的浏览器实例**：已启动账号继续在官方 Web 端正常收发，仅翻译不可用（按 P0-T-08 处理），界面给出「翻译服务不可达，正在重连」的**非阻断**提示；只有当端口占用已因超时被回收（P0-C-20 AC8）或确认团队已到期/被禁用时，才按 AC7 退出登录。**降级而不阻断**，与 P0-M-05 AC14 同一原则——翻译停了是降级，浏览器被关是停工。

---

#### P0-A-02 服务端接口身份认证与凭据续期

**用户故事**：作为平台，我需要所有服务端接口都校验调用方身份，以防止未知身份冒用。

**规则基线**：所有接口都校验身份；凭据**自动续期、客服全程无感知**；**主管禁用密钥后 ≤ 5 分钟**该客服必须真正失去访问权；限流按**单客户端**维度施加，与翻译并发上限是两套独立机制。

> **技术方案需回答**：凭据有效期与续期时机、限流阈值与算法。产品侧只要求三件事——续期对客服不可见（AC2、AC12、P0-A-01 AC2）、禁用后 5 分钟内真的断掉（AC8）、单客户端限流不得波及同团队其他客服（AC11）。

**正常路径**
- AC1 当客户端携带有效凭据调用任一服务端接口，则请求被正常处理。
- AC2 当凭据接近有效期，则客户端在后台自动续期，续期过程**对客服无感知**，进行中的会话与翻译不中断。
- AC3 当凭据完成续期，则旧凭据立即失效，使用旧凭据的请求返回 401。

**异常路径**
- AC4 当请求未携带任何身份凭据，则返回 401，且不执行任何业务逻辑、不产生用量计量。
- AC5 当凭据签名无效或已被篡改，则返回 401 并记录一条安全审计日志。
- AC6 当凭据已过期，则返回 401 且响应中区分「过期」与「无效」两种原因；客户端对「过期」触发一次静默重新续期，续期失败才要求重新登录。
- AC7 当自动续期在网络中断期间失败，则客户端在凭据过期前持续重试；当凭据已过期且续期仍未成功，则客户端进入**降级运行**（P0-A-01 AC15）——已启动账号继续可用、翻译不可用、后台持续重试续期；直到端口占用因超时被回收（P0-C-20 AC8）或确认团队已到期/被禁用，才退出到登录页。
- AC8 当主管禁用某密钥，则该密钥已签发的凭据在 **5 分钟内**失效，后续请求返回 403。
- AC9 当同一凭据从与绑定记录不匹配的硬件指纹发起请求，则服务端拒绝该请求并记录审计日志。
- AC10 当**单个客户端**的请求频率超过限流阈值，则返回 429 并携带可重试时间，客户端据此延后重试而非立即重发。
- AC11 当某个客户端触发限流，则不影响同团队其他客户端的请求处理（限流为单客户端维度，非团队或全局维度）。
- AC12 当凭据在自动续期中被更换，则**不得导致任何一次正在进行的业务请求失败**。此条是硬要求而非优化项——一次因轮换而失败的翻译会计入 P0-T-08 的连续失败计数，累积到 10 次即熔断**整个客户端**；轮换如何做到无缝由后端设计文档决定。

---

#### P0-A-19 后台账号体系与登录

**用户故事**：作为主管/管理员，我需要一个账号登录后台，才能管理密钥或团队。

**规则基线**：后台账号采用**邮箱 + 密码**，邮箱仅作账号标识、**不做邮箱验证**；与客户端密钥是**两套独立体系、互不通用**。**平台管理员全系统只有 1 个**；**每个团队只有 1 个主管账号**，归属唯一团队。后台账号**不能删除，只能禁用**。

**密码与锁定策略**：密码长度 ≥ 8 位且必须同时包含字母与数字（不强制特殊字符，不做定期改密）；**连续登录失败 10 次锁定该账号 30 分钟**。

**正常路径**
- AC1 当系统首次部署，则初始化**唯一的**平台管理员账号（邮箱与初始密码由部署配置提供）；系统**不提供**新增管理员账号的功能。
- AC2 当管理员创建团队，则**同时为该团队创建唯一的主管账号**（邮箱 + 初始密码），初始密码仅在创建时展示一次。
- AC3 当主管以正确的邮箱密码登录用户后台，则签发后台会话凭据，且只能进入其所属团队的数据范围。
- AC4 当主管或管理员登录后主动登出，则该会话凭据立即失效。
- AC5 当主管或管理员修改自己的密码，则修改成功后其**其余已登录会话全部失效**，当前会话保留。

**异常路径**
- AC6 当邮箱不存在或密码错误，则统一提示「邮箱或密码错误」，**不区分是哪一项错**（防止账号枚举）。
- AC7 当同一账号**连续登录失败达 10 次**，则该账号被锁定 **30 分钟**，锁定期间即使输入正确密码也拒绝登录，并提示剩余锁定时间。
- AC8 当账号处于锁定期内再次尝试登录，则**不延长**锁定时长（避免被人为持续锁死）；锁定期满后失败计数清零。
- AC9 当某次登录成功，则该账号的连续失败计数立即清零。
- AC10 当设置或修改的密码不满足「长度 ≥ 8 且同时含字母与数字」，则保存被拒绝并明确指出未满足的规则。
- AC11 当后台账号已被禁用，则登录失败并提示账号不可用；其已签发的会话凭据在 5 分钟内失效。
- AC12 当主管所属团队已被禁用或已过到期时刻，则该团队主管**无法登录**用户后台，提示团队不可用。
- AC13 当主管忘记密码，则由管理员在管理后台重置，重置后该主管的**所有已登录会话立即失效**；系统**不提供**任何自助找回密码流程。当管理员自己忘记密码，则只能通过部署侧重置（运维手段），产品内无入口。
- AC14 当管理员尝试删除后台账号，则系统不提供删除能力，仅提供禁用。
- AC15 当有人尝试禁用平台管理员账号，则操作被拒绝——全系统唯一的管理员账号不可禁用，否则系统将无人可管理。
- AC16 当任何接口的响应中出现密码明文或可还原的密码信息，则视为缺陷；密码在存储中同样**不得以明文或可还原的形式存在**（具体算法由后端设计文档决定）。此条作为安全回归用例常驻。
- AC17 当有人拿后台会话凭据去调用客户端接口（或反之），则请求被拒绝——两套凭据体系互不通用。

---

#### P0-C-18 渠道账号的环境配置（代理与指纹隔离）

**用户故事**：作为客服，我想为每个 IM 账号单独配置账号名、代理和浏览器指纹，让多个账号之间互相隔离，避免被渠道判定为同一环境下的批量操作。

**为什么是 P0**：同一台机器上同时登录多个渠道账号，若共用出口 IP 与浏览器指纹，极易触发渠道风控导致封号——那样前面所有功能都无从谈起。环境隔离是多账号能长期在线的前提。

**配置项**

| 配置项 | 内容 | 必填 |
|--------|------|------|
| **账号名称** | 客服自定义的别名/备注，用于在账号列表中区分同一渠道的多个账号 | 是 |
| **渠道** | Telegram / WhatsApp | 是 |
| **代理** | 类型（SOCKS5 / HTTP(S)）、主机、端口、用户名、密码 | 否（不填则走本机直连） |
| **浏览器指纹** | 具体参数清单**由技术方案决定**，PRD 不锁定；必须提供**一键随机生成** | 否（不填则由系统生成一套并固定下来） |
| **数据目录** | 该账号独立的浏览器用户数据目录，由系统分配，客服不可见不可改 | 系统分配 |

**正常路径**
- AC1 当客服在某个渠道下新增账号，则**立即在该渠道的账号列表中出现一条「未保存」的草稿**并自动选中、同时打开环境配置；客服填写账号名称、代理与指纹后**保存**，该条目才转为正式账号、状态为「未启动」、不占端口。**渠道不需要客服选择**——新增发生在哪个渠道下，账号就属于哪个渠道。
- AC2 当客服点击「一键随机生成」，则系统生成一套完整指纹并填入配置项，客服可在保存前查看与微调。
- AC3 当客服未填写指纹，则系统自动为该账号生成一套并**持久保存**，此后每次启动都复用同一套，不得每次启动都变。
- AC4 当系统生成指纹时该账号已配置代理，则生成的指纹中与地理位置相关的项（时区、语言、区域等）**必须与代理出口地一致**，不得出现「代理在德国、指纹是中国时区」这类错位组合。
- AC5 当客服启动该账号，则浏览器实例按其自身的代理、指纹、独立数据目录创建，**不与其他账号共享 Cookie、LocalStorage 与缓存**。
- AC6 当客服在配置界面点击「测试代理」，则系统发起一次连通性测试并返回结果与出口 IP，供客服确认代理生效。
- AC7 当客服修改已有账号的配置，则提示该配置在**下次启动时生效**；若账号当前已启动，则明确告知需先停止再启动。

**异常路径**
- AC8 当代理不可达、超时或鉴权失败导致浏览器实例未能建立，则启动失败，账号**保持「未启动」状态、自始至终不占用端口**（与 P0-C-03 AC9 的区别：那里实例已建立并进入 Web 登录界面，因此已占用端口），并展示可区分的失败原因（连接失败 / 认证失败 / 超时）。
- AC9 当代理在账号运行中断开，则该账号按 P0-C-03 AC10 置为「离线」，**不得自动回落到本机直连**——宁可离线，也不能让真实 IP 泄漏到渠道侧。
- AC10 当客服配置了代理但测试结果显示出口 IP 与本机直连 IP 相同，则界面明确告警代理未生效，不得静默放行。
- AC11 当已生成的指纹与后来修改的代理出口地不一致（先配指纹、后改代理），则保存时给出**地区错位告警**并提供「按新代理重新生成指纹」的一键操作。
- AC12 当无法探测到代理的出口地（测试失败或地理库无记录），则指纹照常生成，但界面提示地区一致性未能校验，不阻断保存。
- AC13 当两个账号配置了完全相同的代理与指纹，则保存时给出重复告警（不阻断，由客服自行决定），提示存在关联风险。
- AC14 当客服填写的代理格式非法（主机/端口/类型不合法），则保存时即校验拦截并指明具体字段，不等到启动时才报错。
- AC15 当客服删除某账号配置，则同时清理其独立数据目录与本地登录态；删除前明确提示该操作会导致该账号需要重新扫码登录。
- AC16 当账号已启动时客服尝试删除其配置，则操作被拒绝并提示需先停止账号。
- AC17 当系统为账号分配数据目录失败（磁盘满、无写权限），则启动失败并给出明确原因，不占用端口。
- AC18 当客户端在多台设备上使用同一密钥（多开场景），则各设备的账号配置**互相独立**，不做同步。
- AC19 当草稿账号**尚未保存**，则**不允许启动**（启动入口置灰并说明原因），也不申请端口；此时关闭配置界面须二次确认，确认后**丢弃该草稿**、不在列表中留下空条目。
- AC20 当已存在一条未保存的草稿而客服再次点击「新增」，则**不再追加新草稿**，而是跳回那条未填完的——否则列表会堆出一串没有名称、无法区分的空账号。

---

#### P0-C-03 渠道账号的添加、启动与扫码登录（端口占用）

**用户故事**：作为客服，我想在客户端添加并启动我的 Telegram / WhatsApp 账号，通过扫码登录官方 Web 端，以便开始接待客户。

**规则基线**：一个**已启动**账号 = 一个浏览器实例 = 一个端口；端口在**浏览器实例成功建立时**占用、**停止账号时**释放；**本产品不对单客户端的实例数量设上限**；团队端口配额由**服务端裁决**（见 P0-C-20）。

**正常路径**
- AC1 当客服添加一个 IM 账号配置（含 P0-C-18 的环境配置），则该账号出现在账号列表中，状态为「未启动」，**不占用端口**。
- AC2 当客服启动某账号且服务端确认团队仍有可用端口，则客户端**按该账号的代理、指纹与独立数据目录**打开一个浏览器实例并加载对应渠道的官方 Web 端，端口占用数 +1，账号状态变为「等待扫码」。
- AC3 当官方 Web 端展示登录二维码，则客服直接在该实例的页面上扫码（二维码由 Web 端自身渲染，客户端不做二次呈现）。
- AC4 当客服完成扫码且渠道确认登录，则账号状态变为「在线」，客户端获取并展示该账号的手机号、用户名、头像。
- AC5 当账号在线且客户端重启，则若 Web 端登录态仍有效，账号在启动后自动恢复在线，不要求重新扫码。
- AC6 当客服停止某账号，则对应浏览器实例关闭，端口占用数实时 -1，账号状态回到「未启动」。

**异常路径**
- AC7 当团队可用端口数为 0，则启动新账号的操作被拒绝，提示「端口已用尽，请联系主管」，且不创建浏览器实例、不占用端口。
- AC8 当二维码在客服扫码前超时失效，则 Web 端自行刷新二维码，账号保持「等待扫码」与端口占用，不需要客服重新启动账号。
- AC9 当扫码后渠道侧登录失败，则账号状态变为「离线」并展示渠道返回的原始提示；此时浏览器实例已建立，**端口保持占用**，直至客服主动停止账号。
- AC10 当账号在使用中与渠道断开（原因不区分），则账号状态统一变为「离线」，**端口保持占用**；客服可选择停止账号释放端口，或重新启动账号重新扫码登录。
- AC11 当账号处于「离线」状态时客服尝试发送消息，则发送被拒绝并提示账号不在线。
- AC12 当客户端进程被强制杀死后重启，则所有账号回到「未启动」状态，客户端**向服务端上报本机端口占用归零**，不得残留孤儿浏览器实例。
- AC13 当获取账号信息（手机号/用户名/头像）中的任一字段失败，则账号仍可正常收发消息，缺失字段以占位展示，不阻断主流程。
- AC14 当客服在一台机器上启动大量账号，则**本产品不施加任何数量上限**——能开多少由该机器的 CPU / 内存 / 网络自行决定。团队买了 100+ 个端口就想在一台机器上全开，只要机器扛得住，本产品不干预。（因资源不足导致实例创建失败时，按 P0-C-18 AC17 给出明确原因、不占端口。）

> 本版**不做**浏览器实例崩溃后的自动重启与登录态恢复；实例异常终止时账号按 AC10 置为「离线」，由客服手动重新启动。

---

#### P0-C-20 端口占用的服务端裁决与回收

**用户故事**：作为团队主管，我需要团队的端口配额在任何情况下都算得准，不会因为客服关机或客户端崩溃而被永久占满。

**为什么单列**：端口配额是**团队级**的，但端口占用发生在**各个客户端本地**。若只靠客户端自己记账，一旦客户端非正常退出（关机、崩溃、断网后再不启动），服务端会永远认为那些端口还被占着，团队可用端口只减不增，最终无法启动任何账号。

**规则基线**：端口占用以**服务端记录为准**；客户端须持续向服务端**证明自己还在线**，**证明中断超过 24 小时即自动回收**。

产品侧只承诺两个上限——它们都是主管看得见、会据此报障的：

| 承诺 | 上限 |
|------|------|
| 卡死的端口最迟多久能被重新用上 | **24 小时**；客服重启客户端（AC7）或主管手动释放（AC11）则立即 |
| 服务端与客户端对同一个端口的认知，最多不一致多久 | **5 分钟** |

> **技术方案需回答**（本 PRD 不预设答案）：客户端以什么方式证明自己在线（定期续租 / 长连接 / 其他）、间隔多少、失败如何重试。另需注意上述两个上限须**可配置**，否则 QA 无法构造「24 小时超时」这条验收路径。

**正常路径**
- AC1 当客户端启动某账号，则先向服务端申请端口；服务端在团队仍有可用端口时批准并记录占用，客户端才创建浏览器实例。
- AC2 当客户端持有端口占用且保持在线，则该占用持续有效，服务端不得回收。
- AC3 当客服停止账号，则客户端向服务端释放端口，服务端立即回收，团队可用端口数实时 +1。
- AC4 当客户端正常退出，则一次性释放本机持有的全部端口占用。

**异常路径**
- AC5 当持有某端口占用的客户端**连续 24 小时未能证明自己在线**，则服务端**自动回收该端口**，团队可用端口数相应增加。
- AC6 当客服关机、客户端崩溃或断网后不再启动，则其占用的端口**最迟在 24 小时后**被自动回收，不需要任何人工介入；若该客服期间重新启动过客户端，则按 AC7 **立即回收**、不必等满 24 小时——AC7 才是日常路径，AC5/AC6 只兜底「再也不回来」的情况。
- AC7 当客户端重启并上报本机端口占用归零（P0-C-03 AC12），则服务端立即回收该客户端此前的全部占用，不必等到 24 小时超时。
- AC8 当客户端因断网或**服务端不可达**导致中断、随后恢复，则若中断**未超过 24 小时**，占用继续有效、**账号完全不受影响、浏览器实例不得关闭**；若占用**已因超时被回收**，则客户端**关闭对应浏览器实例并将账号置为「未启动」**，提示端口已被回收、需重新启动——**不得置为「离线」**，因为「离线」仍计为占用端口，会与服务端已回收的记录对不上。
- AC9 当服务端记录的团队占用数与该团队所有在线客户端上报之和不一致，则以**服务端记录为准**对外展示，并记录一条告警日志供排查。
- AC10 当端口申请与释放并发发生导致超卖风险，则服务端保证团队占用数**永不超过配额**，超出的申请一律拒绝。
- AC11 当某个端口占用确认卡死（客服设备已报废、长期不再上线）而主管/管理员不愿等满 24 小时，则后台提供**手动释放**入口立即回收该占用；提交前明确提示「该客服可能仍在使用该账号」，释放后按 AC12 由客户端收尾。
- AC12 当某个端口占用已被服务端回收或撤销（超时、手动释放、配额下调），而持有它的客户端仍在运行，则客户端**在 5 分钟内感知到占用已失效**，按 AC8 关闭对应浏览器实例并将账号置为「未启动」——服务端与客户端的认知**不得长期处于相反状态**。

---

#### P0-M-05 页面注入与消息处理

**用户故事**：作为客服，我想在官方 Web 端原有的使用方式上，直接看到每条消息的译文。

**职责边界（本需求的前提，务必先读）**

| 由**渠道官方 Web 端**负责 | 由**本产品客户端**负责 |
|--------------------------|----------------------|
| 会话列表、会话切换、消息列表与渲染 | 渠道与账号的切换（浏览器实例之间） |
| 消息排序、历史消息加载、已读未读 | 在页面中**注入**：识别文本消息、提取原文 |
| 非文本消息（图片/语音/文件/贴纸/位置）的收发与展示 | 调服务端翻译，把译文**注入**到对应消息旁 |
| 模板消息、交互按钮、富文本的渲染 | 发送时在输入框中用译文替换原文并触发发送 |
| 消息撤回、编辑、发送与投递 | 把 原文/译文/语言/翻译状态 写入**本地消息库** |

**客户端不重新实现消息列表**，页面上出现哪些消息、以什么顺序出现，一律以官方 Web 端为准。

**规则基线**：翻译对象为**文本消息**（最小公共集，不含非文本）；单条消息长度上限 **5000 字符**；消息区**固定双语展示**（原文 + 译文）。

**消息标识（去重的基础）**

> 本地消息库中每条消息必须有一个**全局唯一且稳定**的标识。生成方式由技术方案决定，但必须满足：**同一条渠道消息在任意时刻、任意次被注入脚本识别，都得到完全相同的标识**——跨页面重绘、跨滚动回退、跨客户端重启、跨重新登录均成立。
> 这是去重的唯一依据；标识若不稳定，会直接导致同一条消息被反复翻译并**反复计费**。

**正常路径**
- AC1 当客服在客户端切换到某个已启动账号，则界面呈现该账号浏览器实例中的官方 Web 端页面，会话列表与会话切换由该 Web 端提供。
- AC2 当页面上出现一条**收到的**新文本消息，则注入脚本识别该消息、提取原文，并按 P0-T-07 发起翻译。（**发出的**消息不走此路径，译文在 AC5 发送时已取得，回显规则见 AC19。）
- AC3 当译文返回，则注入脚本将译文注入到该条消息对应的位置，与原文一并展示。
- AC4 当某个未被选中的账号有新消息，则客户端在该账号的入口上展示未读标记（未读状态读取自该账号 Web 端页面），供客服自行切换过去查看。
- AC5 当客服在输入框输入内容并触发发送，则客户端先取得译文，**用译文替换输入框内容后再触发 Web 端发送**，本地同时留存原文与译文。
- AC6 当一条消息被处理，则写入本地消息库，**至少含原文、译文、源语言、目标语言、翻译状态**（这五项是双语展示与用量对账的依据，缺一不可）；同时按 §2.4 第 5 条为未来同步预留必要字段，**v1.0 只写不用**。

**异常路径**
- AC7 当页面上出现非文本消息或渠道特有类型消息（图片、语音、文件、贴纸、位置、模板消息、交互按钮、富文本），则由 Web 端原样渲染，客户端**不提取、不翻译、不注入译文、不入本地库**，且不得干扰其正常显示；**Web 端自带的收发能力照常可用**，客户端不做任何限制或拦截。
- AC8 当同一条消息被注入脚本重复识别（页面重绘、滚动回退、重新登录后重新加载），则按消息标识在本地库去重，**不重复发起翻译、不重复计量**，且同一条消息上不得注入出多份译文。
- AC9 当消息内容为空文本、纯 emoji 或纯 URL，则按 P0-T-07 AC8 跳过翻译，页面保持原样、不注入译文。
- AC10 当单条消息超过 5000 字符，则完整存入本地库不截断，译文按 P0-T-07 AC10 完整注入——**客户端不感知服务端如何处理超长文本**。
- AC11 当译文尚未返回，则页面上该条消息保持 Web 端原样可读，**不得因等待译文而遮挡、延迟或改变原文的显示**。
- AC12 当翻译失败，则在该条消息处注入可见的失败标记与手动重试入口（P1-T-13），原文显示不受影响。
- AC13 当发送路径上翻译失败或配额耗尽，则按 P0-T-07 AC11 / AC12 弹窗由客服决定是否发送原文；客服选择取消时，**输入框内容保持为客服原始输入，不得被清空或被半成品译文覆盖**。
- AC14 当 Web 端页面结构发生变化导致注入脚本无法识别消息，则客户端给出「翻译功能异常」的明确提示，**Web 端本身的收发功能不受影响**，客服仍可无译文继续工作。
- AC15 当本地消息库写入失败（磁盘满、文件被占用、数据库损坏），则客户端给出明确错误提示并停止继续写入；**Web 端页面上的消息不受影响**，损失仅限本地留存与译文注入。
- AC16 当对端撤回一条消息，则该消息由 Web 端自行移除，客户端**同步移除已注入的译文**并将本地记录标记为已撤回，页面上不得残留孤立的译文。
- AC17 当一次翻译的译文最终无处可用（消息被撤回、客服已切走账号或会话、客户端已退出），则客户端仅放弃回填；**只要服务端已返回成功，该次翻译照常计入用量**（口径见 P0-B-10）。
- AC18 当客服在译文注入完成前关闭客户端，则重启后该消息在本地库中处于明确终态（已翻译或翻译失败），不得长期停留在「翻译中」。
- AC19 当客服发出的消息随后出现在页面上（Web 端渲染出来的是**已被替换的译文**），则客户端把这条 DOM 消息与 AC5 已写入的本地记录**绑定为同一条**（按发送顺序与内容对齐），并在该消息处**注入客服的原始输入**作为对照——即**发送方向注入的是「原文」，接收方向注入的是「译文」**。此路径**不得再次调用翻译接口**，也不得在本地库产生第二条记录。
- AC20 当发出消息与本地记录**对齐失败**（页面渲染异常、发送被 Web 端拒绝、内容被渠道改写），则该消息在页面上保持 Web 端原样（只有译文、无原文对照），本地记录标注「未对齐」；**不得因对齐失败而重新发起一次翻译**，避免同一条消息被计量两次。

---

#### P0-T-06 会话目标语言的自动识别

**用户故事**：作为客服，我不想手动选语言，希望系统自动判断发出去的消息该翻成什么语言。

**适用范围**：本需求只决定**发送方向**的目标语言。接收方向的目标语言恒为**中文**（客服工作语言，§2.5），不受本需求影响。

**规则基线**：**按联系人发来的最新一条消息的语言**自动识别；若联系人从未发过消息，则**默认英文**。

**正常路径**
- AC1 当联系人已发送过消息，则该会话的目标语言取其**最新一条消息**的识别语言，并在会话内可见当前目标语言。
- AC2 当联系人发来新消息且识别出的语言与当前目标语言不同，则该会话目标语言更新为新语言，后续发出的消息按新语言翻译。
- AC3 当联系人从未发送过消息（客服主动发起的会话），则该会话目标语言为**英文**。

**异常路径**
- AC4 当消息语言识别失败或置信度不足，则沿用该会话上一次的目标语言；若无历史目标语言，则回退为**英文**。
- AC5 当目标语言在会话进行中发生切换，则**历史消息已有的译文不重译**，仅对切换之后发出的消息生效。
- AC6 当识别出的目标语言**两个翻译引擎都不支持**（判定见 P0-S-12 AC12），则该会话的发送翻译标记失败并提示语言不支持，不进行无意义的重试；**若只有其中一个引擎支持，则路由到该引擎照常翻译，不得判失败**。
- AC7 当同一会话在任意时刻被查询目标语言，则返回值稳定一致，不因界面刷新或客户端重启而变化。

---

#### P0-T-07 收发消息自动翻译

**用户故事**：作为客服，我想让收到和发出的消息都自动翻译，以便直接读懂客户并用客户的语言回复。

**规则基线**：每条消息调一次翻译接口；翻译方向见 §2.5（收 → 中文，发 → 会话目标语言）；**发送给客户的是译文**，客服侧**固定双语展示**；跳过翻译的情形不计用量；历史消息**按可视区域按需翻译**。

**正常路径**
- AC1 当页面上出现一条收到的文本消息，则调用服务端翻译接口一次翻译为**中文**，译文注入到该消息处，原文与译文同时可见。
- AC2 当客服发送一条消息，则先**识别客服输入内容的语言**，再翻译为该会话的目标语言，**发送给客户的是译文**，客服侧同时展示自己输入的原文与实际发出的译文。
- AC3 当翻译成功，则该消息翻译状态置为 `succeeded`，并记录源语言与目标语言。
- AC4 当客服滚动查看历史消息，则**仅对进入可视区域的消息**发起翻译，未进入可视区域的历史消息不触发翻译请求。

**异常路径**
- AC5 当翻译接口超时，则翻译状态置为失败并触发 P0-T-08 重试；**原文必须已经可见**，不得因翻译未完成而不展示消息。
- AC6 当翻译接口返回 429，则按退避重试，且不得对同一条消息并发发起多次翻译请求。
- AC7 当翻译接口返回 5xx，则按 P0-T-08 重试；重试耗尽后标记「翻译失败」并提供手动重试入口。
- AC8 当源语言与目标语言相同（联系人本就发中文，或客服直接用会话目标语言输入），或消息内容为纯 emoji / 纯 URL / 纯数字，则**跳过翻译**，直接展示/直接发送原文，**不计入翻译用量**，且不计入翻译成功率的分母。
- AC9 当源语言检测失败，则接收方向仍按目标中文翻译；发送方向以中文作为源语言兜底后重试，仍失败则标记翻译失败，原文正常展示。
- AC10 当消息长度超过翻译引擎单次请求所能处理的上限，则服务端仍须返回**完整译文**；若无法完整翻译，则整条消息标记为翻译失败，**不得出现译文被静默截断而状态仍为成功**的情况。（如何拆分与合并由后端设计文档决定。）
- AC11 当**发送路径**上的翻译失败，则弹窗由客服决定是否发送原文：选择发送则中文原文直接投递并标记该消息译文缺失；选择取消则消息不发送、保留在输入框。
- AC12 当团队翻译配额已耗尽，则**发送路径**弹窗提示配额耗尽并由客服决定是否发送原文（同 AC11 的交互）；**接收路径不弹窗，仅展示原文、不翻译、不注入译文**，并在界面给出一次「翻译配额已耗尽，请联系主管」的提示。
- AC13 当服务端配置的翻译 API Key 无效或额度耗尽，则返回可区分的错误码，客户端展示「翻译服务不可用，请联系管理员」而非泛化失败提示，且不进行无意义的重试。
- AC14 当同一条消息被重复触发翻译（重试、重开会话、重启客户端、重复滚动进入可视区域），则**复用本地已有译文，不重复调用翻译接口、不重复计量**。
- AC15 当客服快速滚动大量历史消息，则翻译请求受 P0-T-08 的并发上限约束，不得对服务端形成突发洪峰。

---

#### P0-T-08 翻译重试与熔断

**用户故事**：作为客服，我希望偶发的翻译失败能自动恢复，不需要我逐条手动处理。

**规则基线**：一条消息的自动重试**最多持续 40 秒**，超时即给出终态结论、不让客服无限等；**连续 10 次翻译失败**后整个客户端进入熔断；熔断**无自动恢复**，必须客服手动触发探测。**并发、失败计数与熔断状态均为「客户端级」**——同一客户端下所有渠道账号共用一套计数与一个熔断开关。

> **技术方案需回答**：40 秒内重试几次、退避曲线怎么排、翻译请求的并发上限取多少。产品侧只要求三件事——总时长不超过 40 秒（AC1、AC4）、不得对同一条消息并发发起多次翻译（AC6）、快速滚动历史消息时不得对服务端形成突发洪峰（P0-T-07 AC15）。

**正常路径**
- AC1 当一次翻译失败且属于可重试错误（超时、429、5xx、网络中断），则自动重试，**重试总耗时不超过 40 秒**。
- AC2 当重试过程中某次成功，则译文正常回填、状态置为 `succeeded`，剩余重试次数不再执行。

**异常路径**
- AC3 当失败属于不可重试错误（API Key 无效、参数非法、内容被引擎拒绝），则不触发自动重试，直接置为终态失败并给出可区分原因。**「语言不支持」不属于此列**——它是**单个引擎的能力边界**，不是该次翻译的终局：须先按 P0-S-12 AC12 换用支持该语种的引擎，**两个引擎都不支持**才判终态失败。
- AC4 当自动重试累计耗时达到 40 秒仍失败，则消息进入「翻译失败」终态，并提供手动重试入口；手动重试**独立于自动重试**，不计入自动重试预算。
- AC5 当客户端在重试等待期间被关闭，则重启后待重试消息仍能被识别并继续处理，不得永久停留在中间态。
- AC6 当并发翻译请求数达到上限，则新请求**排队等待**、不得突破上限；该上限为**客户端级**（跨账号共享），与服务端的单客户端限流是两套独立机制。
- AC7 当**同一客户端**连续 10 次翻译失败（跨账号累计），则该客户端进入熔断态，**其下所有账号**的后续消息直接标记为失败并显示「翻译服务暂时不可用」，不再发起自动重试。
- AC8 当处于熔断态，则**不做任何自动探测、不自动恢复**，熔断状态持续到客服手动触发重试为止。
- AC9 当处于熔断态时客服手动触发重试，则发起一次探测请求；探测成功则**整个客户端**退出熔断态、恢复自动翻译，探测失败则维持熔断态。
- AC10 当失败计数因中间有成功而中断，则连续失败计数清零，不得累积触发误熔断。

---

#### P0-B-09 用户后台：密钥管理与设备解绑

**用户故事**：作为团队主管，我想生成并管理发放给客服的密钥、管理设备绑定，以控制谁可以接入客户端。

**规则基线**：**一个密钥即一个客服**；**密钥数量无配额**，与端口配额是两套独立机制；密钥**不能删除，只能禁用**；解绑/换机由**主管**操作，**无次数限制**。

**正常路径**
- AC1 当主管生成密钥，则系统产出唯一密钥并展示状态（未激活）、创建时间、多开开关（默认关闭）。
- AC2 当某密钥被客服激活，则后台状态更新为已激活，并展示绑定设备列表与各设备激活时间。
- AC3 当主管禁用某密钥，则该密钥不能再用于新激活，且已激活客户端在 5 分钟内失效（P0-A-02 AC8）。
- AC4 当主管对某设备执行解绑，则该绑定记录移除，密钥可在新设备重新激活；解绑次数不受限制。
- AC5 当主管开启某密钥的多开，则该密钥最多可绑定 5 台设备。

**异常路径**
- AC6 当主管生成密钥的数量增加，则不受任何数量配额限制（密钥数量与端口配额互不影响）。
- AC7 当主管禁用一个正在被使用的密钥，则提交前明确提示该操作会导致对应客服在 5 分钟内下线。
- AC8 当主管尝试删除密钥，则系统不提供删除能力，仅提供禁用；已禁用密钥的历史记录与用量数据保留可查。
- AC9 当主管关闭一个已绑定多台设备的密钥的多开开关，则系统**要求主管明确选择保留哪一台、下线哪些设备**，在主管未做出选择前不提交变更。
- AC10 当被选择下线的设备正在线，则该设备在 5 分钟内退出登录并跳转至登录页。
- AC11 当主管解绑一台正在线的设备，则该设备在 5 分钟内退出登录并跳转至登录页。
- AC12 当同一主管账号在多个浏览器/标签页并发操作同一密钥，则以服务端最终状态为准，后提交者收到「状态已变更」提示而非静默覆盖。
- AC13 当团队已被禁用，则主管无法登录（P0-A-19 AC12），其密钥操作自然不可达。

---

#### P0-B-10 用户后台：IM 账号 / 端口用量 / 翻译用量

**用户故事**：作为团队主管，我想看到团队已启动的 IM 账号、端口用量和翻译用量，以便控制资源。

**规则基线**：用量**实时**展示；配额变更生效延迟 **≤ 1 分钟**；翻译配额为**团队级累计总量，默认 150 万字符，用满即止、不自动重置**（§2.6）。

**翻译用量计量口径（全文唯一口径，其他章节一律引用此处）**

> **以服务端翻译接口返回成功为准，按该次翻译的原文字符数计量一次。**
> 只要接口返回成功就计量，**与客户端最终是否用得上这份译文无关**——消息被撤回、客服已切走页面、译文回填不到、客户端已退出，均照常计量，不做任何冲抵或退还。
> 反之，未调用接口（跳过翻译）与接口返回失败，一律不计量。

**正常路径**
- AC1 当主管进入 IM 账号页，则展示本团队所有密钥下已添加的渠道账号及其渠道、状态、所属密钥。
- AC2 当主管进入端口用量页，则展示团队端口配额、当前已占用数、可用数，数据以**服务端记录**为准（P0-C-20 AC9）且为实时值。
- AC3 当某客服启动或停止账号，则端口用量页的已占用数实时变化。
- AC4 当主管进入翻译用量页，则展示本团队**累计已消耗字符数**与**配额总量**（默认 150 万字符）及剩余量，数据为实时值。
- AC5 当管理员修改本团队配额，则变更在 **1 分钟内**在用户后台与客户端生效。

**异常路径**
- AC6 当团队尚无任何账号或用量数据，则展示空态说明，而非报错或空白。
- AC7 当端口已占用数达到配额上限，则页面给出明确超限提示，且客服端新启动账号被拒绝（P0-C-03 AC7）。
- AC8 当累计翻译字符数达到配额总量，则**翻译被阻断**（发送路径与接收路径的表现见 P0-T-07 AC12）；该状态**持续到管理员调大配额为止**，不会随时间自动恢复。
- AC9 当翻译请求失败（超时、5xx、被引擎拒绝），则**不计入翻译用量**。
- AC10 当一条消息经过多次重试后成功，则**只计量一次**（按最终成功那次的原文字符数），失败的那几次不计量。
- AC11 当消息被跳过翻译（源语言=目标语言、纯 emoji/URL/数字），则未调用接口，不计入翻译用量。
- AC12 当超长消息翻译成功，则按**原文实际字符数计量一次**——无论服务端内部为此调用了几次引擎，对外都只算一条；当该条最终翻译失败，则不计量。
- AC13 当用量数据接口不可用，则展示加载失败与重试入口，**不得展示为 0** 造成误读。
- AC14 当主管查看数据，则只能看到本团队范围内的数据，跨团队访问被拒绝（权限边界见 §4.1）。
- AC15 当管理员调大配额总量而团队此前已耗尽配额，则翻译在 1 分钟内恢复可用，**累计用量数字不清零**（只是上限变大）。
- AC16 当管理员把配额总量调到低于当前累计用量，则翻译立即被阻断，用量页明确展示「已超出配额」而非展示为负数或归零。
- AC17 当团队被禁用后又恢复启用，则累计用量**延续原值继续累加**，不清零、不重置。
- AC18 当页面展示任何时间，则统一使用 `Asia/Shanghai` 并带时区标识；查看者所在设备的系统时区**不影响**展示结果，也不影响用量计算。
- AC19 当客服在**客户端**查看团队端口与翻译配额，则展示两组**团队级汇总**数字——端口「已占用 / 配额」与翻译「累计已用 / 配额总量」——且数值与用户后台**同源同值**（服务端记录，配额变更后 1 分钟内一致）。此处**只展示团队汇总**：不得出现其他客服的密钥、账号、设备或分项用量（权限例外的边界见 §4.1）。客服看不到余量就只能在停工那一刻才发现资源耗尽，因此这是必要项而非增强项。

---

#### P0-S-11 管理后台：团队配置

**用户故事**：作为平台管理员，我想创建和管理团队，并监控各团队运行状态。

**规则基线**：团队具备**到期时刻**（决定其下所有密钥的有效期与主管的登录权限），**精确到秒**，管理员可随时修改；团队**不能删除，只能禁用**；禁用后数据保留；**到期不提醒**。时间口径见 §2.6。

**正常路径**
- AC1 当管理员创建团队，则系统写入**创建时刻**（精确到秒，不可修改），并由管理员配置**到期时刻**（日期 + 时分秒）、端口配额、翻译配额总量（默认 150 万字符），同时为该团队创建唯一的主管账号（P0-A-19 AC2），团队进入启用状态。
- AC2 当管理员查看团队列表，则可看到各团队的启用状态、**创建时刻、到期时刻**、端口占用、**累计翻译用量与配额总量**；所有时间统一按 `Asia/Shanghai` 渲染并带时区标识。
- AC3 当管理员修改团队配额或到期时刻，则变更在 1 分钟内对该团队的用户后台与客户端生效。
- AC4 当管理员把到期时刻改大（延长有效期），则**只有 `expires_at` 改变**，创建时刻与累计用量均不受影响。
- AC5 当管理员把已到期团队的到期时刻改到未来，则该团队的密钥与主管账号立即恢复可用，累计用量延续原值继续累加（P0-B-10 AC17）。

**异常路径**
- AC6 当管理员把端口配额下调到低于当前已占用数，则变更生效后**客户端弹窗，由客服选择要关闭的 IM 账号**，直至已占用数不超过新配额；在客服完成选择前不得强制关闭正在接待中的账号。
- AC7 当客服在弹窗中拒绝或忽略选择，则客户端持续提示且**不允许启动任何新账号**，直至满足配额。
- AC8 当管理员禁用一个正在运行的团队，则提交前明确提示影响范围（在线客服数、已启动账号数），禁用后该团队客户端按 P0-A-01 AC13 退出、主管按 P0-A-19 AC12 无法登录。
- AC9 当管理员尝试删除团队，则系统不提供删除能力，仅提供禁用；已禁用团队的密钥、账号、用量历史全部保留可查。
- AC10 当系统时间到达团队的**到期时刻**（精确到秒），则该团队所有密钥同时失效、主管无法登录，在线客户端按 P0-A-01 AC7 强制退出；到期时刻前一秒仍完全可用，系统**不做**任何到期前提醒。
- AC11 当管理员把到期时刻改为已过去的时刻，则等同于立即到期，提交前须明确提示影响范围。
- AC12 当管理员尝试修改团队的创建时刻，则系统不提供该能力（创建时刻由服务端写入，一经写入不可变）。
- AC13 当管理员填写的配额值为负数或非整数，则保存时即校验拦截并指明允许范围。

---

#### P0-S-12 管理后台：翻译 API Key 与引擎自动切换

**用户故事**：作为平台管理员，我想集中配置 Google / DeepL 的 API Key，并让系统在某引擎故障时自动切换。

**规则基线**：**每个引擎下可配置多个 API Key**。选路分两层——

| 层 | 选什么 | 依据 |
|---|--------|------|
| 第一层 | 选**引擎** | 在「**可用且支持该次翻译语种**」的引擎中随机选一个；某引擎故障时**自动切换**到另一引擎。**不做流量配比和优先级设置** |
| 第二层 | 在该引擎下选 **Key** | 在该引擎**状态为「可用」的 Key** 中选一个。某个 Key 额度耗尽或失效即自动跳过；**同引擎的 Key 全部不可用，才把该引擎判为不可用**并回到第一层切换 |

> **为什么要加语种这一维**：两个引擎的语种覆盖并不对等（DeepL 约 30 余种，Google 130+）。若只按可用性随机，目标语种一旦只有 Google 支持，约一半的消息会随机命中 DeepL 并被判「语言不支持」而直接失败——而 Google 本来翻得了。

> **为什么一个引擎要挂多个 Key**：单个 Key 的额度是有限的，用满即停。多个 Key 让额度可以横向扩，也让「某个 Key 被封 / 被误改」不至于让整个引擎瞬间不可用。代价是多了一层选路，**具体选 Key 的规则见 OQ-48（待定）**。

**正常路径**
- AC1 当管理员为某引擎添加一个 API Key 并保存，则该 Key 加入该引擎的可用 Key 集合并开始参与翻译；同一引擎下可添加**多个** Key，数量不设上限。
- AC2 当管理员查看已配置的 Key，则界面仅展示掩码片段，不展示完整明文。
- AC3 当两个引擎均可用**且都支持该次翻译的语种**，则服务端在两者中**随机选择一个**完成翻译，管理员无需也无法干预选择。
- AC13 当管理员查看某个 Key，则可看到该 Key 的**实时用量**——已消耗字符数与额度上限、近 24 小时调用次数与失败次数、最近一次使用时刻、最近一次失败原因。用量按 Key 独立统计，与团队维度的翻译用量（P0-B-10）是**两套互不替代的口径**：前者看的是「这个 Key 还能用多久」，后者看的是「这个团队还能翻多少」。
- AC14 当管理员停用某个 Key，则新的翻译请求不再落到它上面，但其**历史用量与统计数据全部保留**；停用可随时撤销。

**异常路径**
- AC4 当保存或轮换的 API Key 无效，则**保存时即通过一次连通性校验返回失败原因**，该 Key 不被保存、线上继续使用原有配置，不等到线上翻译报错才暴露。
- AC5 当任何客户端或用户后台接口的响应中出现 API Key 明文或可反推的片段，则视为缺陷（此条作为安全回归用例常驻）。
- AC6 当管理员轮换 API Key，则新 Key 生效后进行中的翻译请求不因轮换而失败，或失败后可被 P0-T-08 自动重试挽回。
- AC15 当某个 Key 额度耗尽或被判失效，则服务端**自动跳过它**、把请求落到同引擎下仍可用的 Key，**客服侧不感知**；该 Key 在管理后台标记为「额度耗尽」或「Key 失效」并显示最近一次失败原因。
- AC7 当某引擎**所有 Key 都不可用**（额度耗尽 / 失效 / 被停用），则该引擎被标记为不可用，服务端**自动切换到另一引擎**重试该请求，客服侧不感知切换过程。
- AC8 当只剩一个引擎可用，则所有请求都路由到该引擎，不再在引擎间随机选择（该引擎内部仍按第二层规则选 Key）。
- AC9 当两个引擎**均不可用**，则返回可区分的错误码，客户端展示「翻译服务不可用，请联系管理员」，并按 P0-T-08 AC7 进入熔断。
- AC10 当某引擎不可用，则管理后台可见该引擎的不可用状态、其下每个 Key 各自的状态与最近一次失败原因——须能一眼看出是「额度用完了」还是「Key 被改坏了」，这两者的处理动作完全不同。
- AC11 当某个引擎**只配置了一个 Key**，则管理后台明确提示该风险：这个 Key 一旦耗尽或失效，整个引擎立即不可用。同理，当**只配置了一个引擎**，则系统正常工作但不具备引擎间自动切换能力，同样明确提示。
- AC12 当某次翻译的语种**只有其中一个引擎支持**，则服务端**直接路由到支持该语种的引擎**、不做随机选择，翻译照常成功；只有当**两个引擎都不支持**该语种时，才返回「语言不支持」并按 P0-T-08 AC3 置为终态失败。当引擎新增支持的语种时，**须能在不发布客户端新版本的前提下生效**。
- AC16 当某个 Key 的额度即将耗尽，则管理后台的用量展示须让管理员**在耗尽之前就能看出来**（进度可视）——本版不做主动告警推送，但不能等到翻译开始失败才发现。

---

### 5.2 P1 — 应该有（Should Have）

---

#### P1-T-13 翻译失败的手动重试

**用户故事**：作为客服，我想对翻译失败的消息一键手动重试。

- AC1 当客服点击某条失败消息的重试按钮，则对该消息重新发起一次翻译，其他消息不受影响。
- AC2 当手动重试成功，则译文就地回填，状态更新为 `succeeded`。
- AC3 当客户端处于熔断态时点击重试，则该次请求作为探测请求处理（P0-T-08 AC9）。
- AC4 当一条消息正在自动重试中，则其手动重试入口不可点击，避免并发发起重复请求。

---

#### P1-C-15 渠道账号状态可视

**用户故事**：作为客服，我想一眼看到每个账号当前是什么状态、占了几个端口。

**状态集合**（本版只维护这 4 个，不区分掉线原因）：`未启动` / `等待扫码` / `在线` / `离线`

- AC1 当客服查看账号列表，则每个账号展示其当前状态与是否占用端口。
- AC2 当账号状态在上述 4 个状态之间变化，则账号列表立即更新，无需手动刷新。
- AC3 当账号处于「未启动」，则列表标注其不占用端口；当账号处于其余 3 个状态，则标注其占用 1 个端口。
- AC4 当账号处于「离线」，则列表提供「重新启动」与「停止账号」两个操作入口。
- AC5 当账号处于「离线」，则**不展示**掉线原因的细分归类（渠道侧原始提示可原样透传，但系统不做原因判定与分类）。

---

#### P1-B-16 按密钥维度的用量分布

**用户故事**：作为主管，我想按客服（即密钥）维度看到端口与翻译用量分布。

- AC1 当主管进入用量页，则可按密钥拆分展示各自的端口占用数与累计翻译字符数。
- AC2 当某密钥尚无任何用量，则该密钥仍出现在列表中且数值展示为 0，不得缺行。
- AC3 当某密钥已被禁用，则其历史用量仍可查，行内标注已禁用。

---

#### P1-S-17 团队运行状态监控

**用户故事**：作为管理员，我想看到各团队的实时运行状态。

- AC1 当管理员进入监控页，则可看到各团队的在线账号数、端口占用、翻译成功率与失败原因分布。
- AC2 当某团队已被禁用或已到期，则其历史数据仍可查看，行内标注当前状态。
- AC3 当某团队累计翻译用量达到配额总量，则监控页高亮该团队。

---

## 6. 方案概述（Solution Overview）

### 6.1 系统构成

| 组件 | 职责 | 关键约束 |
|------|------|---------|
| **客户端**（Windows / Linux / macOS） | 浏览器实例管理与**环境隔离**（代理/指纹/数据目录）、**页面注入**（提取原文、注入译文、发送时替换输入框内容）、消息本地留存、发起翻译请求、维持端口占用的在线证明 | 跨平台语言；**不重新实现会话/消息列表**；不持有翻译 API Key；消息与账号配置只存本地；需防反编译加固；**不设单机实例数上限** |
| **服务端** | 身份认证与凭据续期、后台账号体系、密钥与硬件绑定、**端口占用裁决与超时回收**、翻译代理与引擎自动切换、用量计量、配额裁决 | 所有接口需身份认证；限流按单客户端维度施加 |
| **用户后台**（主管） | 密钥管理、设备管理与解绑、IM 账号查看、**端口管理（含手动释放）**、翻译用量 | 不接触 API Key；数据范围限本团队 |
| **管理后台**（平台管理员） | 团队管理（有效期/配额/主管账号）、**端口管理（全平台，含手动释放）**、运行监控、**翻译引擎与多 Key 管理（含按 Key 的用量监控）** | 唯一持有 API Key 的界面 |

### 6.2 渠道接入形态

```
客服添加账号配置：账号名 + 代理 + 指纹 + 独立数据目录（未启动，不占端口）
   → 启动账号：向服务端申请端口 → 获批后按环境配置开一个浏览器实例（占用 1 个端口）
   → 实例经该账号自己的代理加载 Telegram Web / WhatsApp Web
   → Web 端展示官方二维码，客服自行扫码
   → 登录成功（在线）：客服在官方 Web 端里正常使用
   → 注入脚本识别页面上的文本消息 → 提取原文 → 调服务端翻译
   → 译文注入回页面（双语展示）+ 写入本地消息库
   → 断开连接（离线，端口仍占用）→ 客服自行「重新启动」或「停止账号」
```

**注入式模型的边界**：客户端是官方 Web 端的「外壳 + 增强层」，不是 IM 客户端的重新实现。会话列表、消息渲染与排序、历史加载、非文本消息、撤回、发送与投递，统统由 Web 端负责；客户端只做三件事——**读页面上的文本、把译文写回页面、把处理过的消息留一份在本地**。

由此形态推导出的关键约束：

1. **端口 = 浏览器实例**，是真实的进程级开销，因此有最低硬件配置要求（按 5 端口计）。但**产品侧不设单机实例数上限**——开得动多少由客户机器决定，本产品不替用户做这个判断。端口是**团队级配额**，必须由服务端裁决并支持超时回收（P0-C-20）。
2. **实例之间必须完全隔离**：每个账号有自己的代理、指纹与数据目录。共用出口 IP 或指纹会让多个账号在渠道侧被关联，直接导致封号——这是多账号能长期在线的前提（P0-C-18）。
3. **登录态由渠道 Web 端持有**，客户端无法绕过扫码，因此账号恢复的路径只有「重新启动 → 重新扫码」。
4. **消息能力受 Web 端限制**：不在最小公共集内的消息不是「不支持收发」，而是「照常收发、只是不翻译」。
5. **注入点依赖 Web 端页面结构**，是本方案最脆弱的一环：Web 端改版可能导致识别失效。因此要求注入失效时**降级而不阻断**（P0-M-05 AC14）——译文没了，但客服还能照常收发。
6. **消息标识必须稳定**：去重与防重复计费全靠它，标识不稳定会直接导致重复翻译与重复扣费（P0-M-05 消息标识条款）。

### 6.3 关键设计决策

1. **注入而非重建**：客户端不自建会话/消息列表，页面呈现一律以官方 Web 端为准，只增强不接管。
2. **可视区域按需翻译**：历史消息不做全量翻译，这是控制服务端压力的主要手段。
3. **只禁用不删除**：团队、密钥、后台账号均无物理删除，保证用量与审计数据可追溯。
4. **端口以服务端记录为准 + 超时回收**：客户端本地计数只用于展示，配额裁决与回收在服务端。「**容忍多久的异常**」（24 小时）与「**多久感知一次状态变化**」（5 分钟）是**两条独立的承诺**，不要因为实现省事就并成一个数。
5. **计量以服务端返回为准**：翻译接口返回成功即计量，与客户端是否用得上译文无关（口径见 P0-B-10）。
6. **降级而不阻断**：翻译链路（服务端、注入脚本、引擎）出任何问题，都不得影响客服在官方 Web 端的收发。译文没了是降级，浏览器被关是停工（P0-A-01 AC15、P0-M-05 AC14）。

### 6.4 状态机（用于测试用例枚举）

**渠道账号状态**（本版只有 4 态，不区分掉线原因）：

```
未启动 ──启动账号──▶ 等待扫码 ──扫码成功──▶ 在线
  ▲                      │                    │
  │                      └────连接断开────────┤
  │                                           ▼
  └──────────停止账号──────────────────────  离线
                                              │
                                    重新启动 ──┘（回到「等待扫码」）
```

- **端口占用**：仅 `未启动` 为 0，`等待扫码` / `在线` / `离线` 均占用 1
- 代理失败导致实例未建立时，账号**停留在 `未启动`**，从未占用端口（P0-C-18 AC8）

**翻译状态**（本地消息库字段，客户端唯一维护的消息状态）：

```
pending → translating → succeeded | failed | skipped
                ▲            │
                └──重试──────┘（自动 ≤40 秒 / 手动不限次）
```

- `skipped`：跳过翻译的终态，**未调用接口，不计用量**
- `succeeded` / `failed`：以服务端返回为准；`succeeded` 即计量，`failed` 不计量

> **不维护消息发送状态机**：消息的发送、投递、失败重发均由渠道官方 Web 端负责并自行呈现。客户端在发送路径上只负责「翻译 → 替换输入框内容 → 触发发送」。

要求：翻译状态的**终态可达性**必须可测——当客户端在 `pending` 或 `translating` 时被强制关闭，则重启后该记录在有限时间内进入终态。

### 6.5 已收敛的技术风险

| # | 原风险 | 当前结论 |
|---|--------|---------|
| R1 | 渠道是否有合法接入路径 | **已解决**：浏览器实例 + 官方 Web 端 + 客服自行扫码。收发拦截可行性**已由技术侧提前验证通过**，且市场上已有同类产品长期运行 |
| R2 | 消息能力不对齐 | **已定**：取各渠道最小公共集，不含非文本消息 |
| R3 | 大量历史消息的翻译压力 | **已定**：可视区域按需翻译 + 客户端级并发上限 + 熔断（上限取值由压测确定，见第 4 周） |
| R4 | 客户端反编译 | **已定**：达到「一般 IT 人员无法轻易反编译」即可，加固方案第 1 周与语言选型一并验证 |
| R5 | 多账号环境隔离（代理 + 指纹）是否做得出来 | **已解决**：技术侧已完成一个**仅含客户端的 demo**，隔离方案已跑通 |
| R6 | 服务端不可达是否会造成全网停工 | **已解决**：端口占用容忍 24 小时的证明中断 + 运行中降级不退出、不关实例（P0-C-20、P0-A-01 AC15） |
| R7 | 两个翻译引擎语种覆盖不对等 | **已定**：按语种能力路由，只有一个引擎支持时直接指定该引擎（P0-S-12 AC12） |
| R8 | 单个 API Key 额度用满导致整个引擎不可用 | **已定**：一个引擎可挂多个 Key，某 Key 耗尽即自动跳过，全部耗尽才判引擎不可用（P0-S-12 AC15）。**引擎内的选 Key 规则待定，见 OQ-48** |

---

## 7. 待决问题（Open Questions）

### OQ-48｜同一引擎下多个 Key 的选择规则（v1.0-rc5 新增，**待产品决策**）

引擎改为可挂多个 Key（P0-S-12）之后，多出了一层「在该引擎的可用 Key 中选哪一个」。**PRD 不预设答案**，因为三种规则在额度快用完时的行为差别很大：

| 候选规则 | 好处 | 代价 |
|---------|------|------|
| **轮询**（依次轮转） | 实现最简单，各 Key 均匀消耗 | 所有 Key 会**几乎同时**用满，届时引擎瞬间整体不可用 |
| **按剩余额度最多优先** | 额度自然被拉平，不会集中耗尽 | 每次选路都要读实时用量，且用量统计的延迟会影响准确性 |
| **按最近失败率避让** | 能自动绕开正在出问题的 Key | 失败率窗口怎么取是新的调参项；额度维度完全不管 |

**这不是技术选型，是产品判断**——它决定的是「额度用完那一刻，是所有 Key 一起断，还是一个一个断」。

**需要谁来定**：产品经理。**什么时候必须定**：第 3 周后端做翻译代理之前。**定完落到哪**：P0-S-12 规则基线第二层，并补一条对应 AC。

---

OQ-01 ~ OQ-47 的结论已全部落入正文：

| 批次 | 结论落点 |
|------|---------|
| OQ-01 ~ OQ-32 | §2 ~ §5 全文 |
| OQ-33 ~ OQ-40 | 限流单客户端维度（P0-A-02）、引擎随机选择（P0-S-12）、熔断无自动恢复（P0-T-08）、目标语言默认英文（P0-T-06）、配额 150 万字符累计（P0-B-10）、后端承担后台前端（§4.3）、单机实例数不设上限（P0-C-03 AC14，v1.0-rc5 修订）、到期不提醒（P0-S-11 AC10） |
| OQ-41 ~ OQ-43 | 不做显示模式切换（NG-14）、规模以配额为准（§4.2）；配额周期规则已随删除周期概念而作废 |
| OQ-44 ~ OQ-45 | 展示时区固定 `Asia/Shanghai`（§2.6、NG-22）；指纹清单交技术方案决定，但必须有一键随机生成且不得与代理地区错位（P0-C-18） |
| OQ-46 | 已随删除配额周期而自动失效 |
| OQ-47 | 后台账号邮箱+密码不做验证、锁定 10 次 / 30 分钟、密码 ≥8 位含字母数字、不做找回密码（P0-A-19、NG-24） |

> 开发过程中新出现的问题，请续接 OQ-49 编号登记，并同步更新本节与 brief。

**与「技术方案需回答」的分工**：正文中标注 `技术方案需回答` 的条目（目前在 §2.4 第 5/9 条、P0-A-02、P0-T-08、P0-C-20）**不属于产品待决问题**，它们是交给**后端设计文档**的输入——PRD 只写「用户/主管看得见的上限与承诺」，「靠什么机制做到」由技术侧定稿，**不回流本 PRD**。产品侧只在这些机制**改变了已承诺的上限**时才需要重新介入。

---

## 8. 时间线与阶段（Timeline & Phasing）

**目标工期：1 个月**。团队为 PM ×1、前端 ×1（客户端）、后端 ×1（服务端 + 两个后台前端）。

### 第 1 周：技术验证 + 骨架

| 角色 | 工作 |
|------|------|
| 产品 | 定稿客户端外壳与两个后台的界面；确认译文在消息气泡中的注入位置 |
| 前端 | 在既有 demo 基础上落地客户端骨架；**消息稳定标识的可行性验证（本周唯一的硬性未知，最高优先）**；多浏览器实例的资源占用实测；跨平台打包与加固方案验证（R4）。扫码登录、收发拦截、代理与指纹隔离已由前期 demo 验证（R1/R5），本周只做工程化 |
| 后端 | 团队/密钥/硬件绑定/后台账号数据模型；身份认证与 24 小时凭据自动续期；后台登录；单客户端限流 |

**出口条件**：当在一台机器上能通过浏览器实例、经指定代理登录任一渠道 Web 端并成功读到一条消息、且该消息在客户端重启后仍被识别为同一条，同时客户端能用密钥激活、主管能登录用户后台，则本周达成。

> ⚠️ 渠道接入本身已验证（R1/R5），本周真正的未知只剩**消息稳定标识**。它若不成立，去重与防重复计费都没有依据（见 P0-M-05 消息标识条款）——**这一项不通过就不要往第 2 周推进**。

### 第 2 周：渠道与消息闭环

| 角色 | 工作 |
|------|------|
| 产品 | 验收第 1 周产出；准备异常路径测试用例（见附录 A） |
| 前端 | **账号环境配置（账号名/代理/指纹/独立数据目录）与代理连通性测试**；账号添加/启动/扫码/停止；端口申请与占用维持；4 态账号状态；渠道与账号切换；**两个渠道 Web 端的注入脚本**；消息本地留存与去重 |
| 后端 | **端口裁决与超时回收**；翻译代理（Google + DeepL）、**引擎选择与引擎内多 Key 选路**、自动切换、用量计量（团队维度 + **Key 维度**） |

**出口条件**：当客服能配置并启动两个使用不同代理与指纹的账号、分别扫码登录、完成一轮收发，两个实例互不共享登录态，且**强杀客户端后重启、其此前占用的端口被服务端立即回收**（P0-C-20 AC7），同时**在调短超时的测试配置下跑通超时回收路径**（AC5），则本周达成。

### 第 3 周：翻译闭环 + 管控面

| 角色 | 工作 |
|------|------|
| 产品 | 验收异常路径，重点验收注入失效时的降级表现（P0-M-05 AC14）；**本周开工前必须给出 OQ-48 的结论**（引擎内多 Key 的选择规则），否则后端的翻译代理无法定稿 |
| 前端 | 双向翻译（收 → 中文 / 发 → 目标语言）；目标语言自动识别；可视区域按需翻译；重试/熔断的界面表现；发送失败与配额耗尽弹窗；译文注入的失败标记与手动重试入口 |
| 后端 | 配额裁决（端口/翻译）、设备解绑、监控数据；**用户后台与管理后台的页面开发** |

**出口条件**：当收发消息均自动翻译成功，注入超时/429/5xx/Key 失效四类故障后系统均按 P0-T-07、P0-T-08 表现，且**同一引擎下配置两个 Key 时，把其中一个的额度打满后请求能自动落到另一个、客服侧全程无感知**（P0-S-12 AC15），则本周达成。

### 第 4 周：联调、异常验证与发布

| 角色 | 工作 |
|------|------|
| 全员 | 端到端联调；按附录 A 逐维度做故障注入；权限边界回归；API Key 与密码泄漏回归 |
| 产品 | 与后端一同确认压测结论：限流阈值、翻译并发上限、重试次数与退避曲线——这些参数由技术定，但**不得突破 PRD 承诺的上限**（重试总时长 ≤ 40s、单客户端限流不波及同事） |
| 前端 | Windows / Linux / macOS 三端打包与加固 |
| 后端 | 基础压测（翻译并发、限流阈值、端口回收） |

**出口条件**：当管理员能完成「创建团队 + 主管账号 → 主管登录生成密钥 → 客服激活 → 启动账号扫码登录 → 收发并双向翻译 → 查看端口与翻译用量 → 禁用密钥使其 5 分钟内下线」的完整闭环，且附录 A 各维度全部通过，则 v1.0 可发布。

### 交付风险

底稿说明**没有外部时间约束**，1 个月是内部目标。在此前提下有三点值得提前想好取舍：

1. **第 1 周的渠道验证是全局前置**。它不通过，后面三周都没有意义；它拖到第 2 周，整体就压到 3 周。建议把它当作 go/no-go 关卡，而不是普通开发任务。
2. **后端 1 人同时承担服务端 + 两个后台前端 + 后台账号体系**，是排期中最薄的一环，集中压力落在第 3 周。建议把两个后台的页面按「先够用、后好看」处理，优先保证 P0 数据准确可见。
3. **若需要压缩范围**，建议的下沉顺序是：P1 全部 → 单渠道先行（先做一个渠道，另一个下版本）。**不建议下沉的是**：端口裁决与回收、翻译用量计量口径、消息标识稳定性、权限边界——这四项一旦做错，后面很难回补。

---

## 附录 A：异常路径测试清单（按故障注入维度）

| 维度 | 需注入的故障 | 覆盖的验收条件 |
|------|------------|--------------|
| 网络 | 断网、高延迟、间歇丢包、DNS 失败、**服务端不可达数分钟（客户端须降级而不退出、不得关实例）** | P0-A-01 AC10/AC14/**AC15**、P0-A-02 AC7、P0-C-20 AC8、P0-T-07 AC5 |
| 服务端 | 503、502、超时、限流 429 | P0-A-02 AC10/AC11、P0-T-07 AC6/AC7、P0-T-08 AC1/AC6/AC7 |
| 认证（客户端） | 无凭据、过期、篡改、续期失败、密钥被禁用、**凭据被拿到另一台机器上用**（指纹与绑定记录不符） | P0-A-02 AC4–AC9 |
| 激活 | 密钥无效 / 已禁用 / 团队到期 / 团队被禁用 / 已在其他设备激活 / 已达 5 台上限 / 并发激活抢最后一个名额 / 激活中断网、**换硬盘或网卡后再激活（须按新设备走名额判定，有名额即成功）** | P0-A-01 AC4–AC13、**AC12/AC16** |
| 认证（后台） | 邮箱/密码错误、连续失败 10 次锁定 30 分钟、锁定期内重试、弱密码、账号被禁用、团队到期后登录、管理员重置密码、尝试禁用管理员、两套凭据串用 | P0-A-19 AC6–AC17 |
| 账号与端口 | 二维码超时、扫码失败、连接断开、实例异常终止、客户端进程被杀、单机启动大量账号至资源耗尽（须给出明确原因、不占端口） | P0-C-03 AC7–AC14、P1-C-15 |
| 端口回收 | 客户端强杀不再启动、断网/服务端不可达（24 小时内账号不受影响）、超时被回收后客户端恢复（账号须回到「未启动」）、**主管手动释放**、**占用被撤销后客户端 5 分钟内感知**、并发申请超卖、服务端与客户端计数不一致 | P0-C-20 AC5–AC12 |
| 环境隔离与账号配置 | 代理不可达/鉴权失败/运行中断开、代理未生效（出口 IP = 本机 IP）、指纹与代理地区错位、出口地探测失败、代理格式非法、两账号配置重复、数据目录分配失败、**草稿未保存即尝试启动**、**放弃草稿**、**已有草稿时再次点新增** | P0-C-18 AC4/AC6/AC8–AC17、**AC19/AC20** |
| 注入 | 重复识别同一条消息、消息标识跨重启不稳定、Web 端页面结构变更、译文注入前切走页面、注入脚本加载失败、**发出消息的原文回显与对齐失败** | P0-M-05 消息标识条款、AC8/AC11/AC14/**AC19/AC20** |
| 消息类型 | 非文本消息、模板消息/交互按钮/富文本、空文本、纯 emoji/URL、超长消息、对端撤回 | P0-M-05 AC7/AC9/AC10/AC16、P0-T-07 AC8 |
| 翻译方向 | 联系人发中文、联系人语言中途切换、**客服直接输入英文或目标语言**、源语言识别失败、目标语言引擎不支持 | §2.5、P0-T-06 AC2/AC5/AC6、P0-T-07 AC2/AC8/AC9 |
| 翻译引擎与 Key | Key 无效、**单个 Key 额度耗尽（须自动跳到同引擎其他 Key、客服无感知）**、**同引擎所有 Key 均不可用（才切换引擎）**、双引擎均故障、Key 轮换、停用后再启用、超长消息（须完整返回或整条判失败）、**语种仅单引擎支持（须路由而非判失败）**、**语种双引擎均不支持** | P0-T-07 AC10/AC13、P0-S-12 AC4/AC7–AC16、P0-T-06 AC6、P0-T-08 AC3 |
| 熔断 | 跨账号累计失败触发熔断、熔断期间其他账号收发、手动探测成功/失败、失败计数被成功打断 | P0-T-08 AC6–AC10 |
| 配额 | 端口用尽、端口配额下调至低于占用、翻译配额耗尽（收发两个方向）、调大配额恢复、配额调到低于已用量、团队到期与禁用 | P0-C-03 AC7、P0-T-07 AC12、P0-B-10 AC7/AC8/AC15–AC17、P0-S-11 AC6–AC12 |
| 本地环境 | 磁盘满、数据库损坏、进程被强杀 | P0-M-05 AC15/AC18、P0-T-08 AC5、§6.4 |
| 计量口径 | 撤回后返回成功、切走页面后返回成功、重试后成功、分片部分失败、跳过翻译 | **P0-B-10 计量口径条款**、AC9–AC12、P0-M-05 AC17 |
| 并发 | 同密钥并发激活、同一主管账号多标签页并发操作、快速滚动历史 | P0-A-01 AC11、P0-B-09 AC12、P0-T-07 AC15 |
| 权限 | 客服越权查他人数据、主管跨团队访问、后台接口泄漏 API Key 或密码、**客户端展示团队配额时混入他人密钥/账号/分项用量** | §4.1、P0-B-10 AC14/**AC19**、P0-S-12 AC5、P0-A-19 AC16 |

---

## 附录 B：变更记录

| 版本 | 主要变更 |
|------|---------|
| v1.0-rc5 | **原型评审反馈的五处修订**：①**P0-A-01 AC12 重写**——硬件指纹变化（换硬盘/网卡、迁移虚拟机）不再是一条独立失败路径，一律按「一台新设备」走名额判定，有名额即激活成功、没名额才按 AC8/AC9 失败；新增 AC16 明确原设备绑定记录仍占名额、须提示联系主管解绑。②**取消单客户端 100 实例上限**（§4.2、P0-C-03 规则基线与 AC14、§6.1、§6.2 约束 1、§7、附录 A 共 6 处）——买了 100+ 端口就想在一台机器上全开，只要机器扛得住，本产品不干预。③**§4.1 增补客服可见范围的唯一例外**——团队端口与翻译配额的**余量**对客服可见（仅团队级汇总，不含他人明细），配套新增 P0-B-10 AC19；理由是这两项是团队共用的消耗性资源，看不见余量就只能在停工那一刻才发现。④**P0-C-18 AC1 重写 + 新增 AC19/AC20**——新增账号先落一条「未保存」草稿，**保存后才能启动**，未保存不申请端口、放弃即丢弃、已有草稿时不再追加新草稿；渠道不需客服选择，新增发生在哪个渠道下就属于哪个渠道。⑤**P0-S-12 改为一个引擎可挂多个 API Key**——选路分两层（选引擎 / 引擎内选 Key），新增 AC13（每个 Key 的实时用量与调用失败统计）、AC14（停用可逆、历史保留）、AC15（Key 耗尽或失效自动跳过、客服无感知）、AC16（额度耗尽前可见），改写 AC7/AC8/AC10/AC11。**引擎内的选 Key 规则未定，登记为 OQ-48** |
| v1.0-rc4 | **把底稿里的调优参数一并交给后端设计文档**（底稿是起点不是终点）：①§2.4 第 5 条同步字段清单 → 改为「数据模型须为未来同步留出空间，将来不必迁移历史消息」，P0-M-05 AC6 同步改写，但**保留原文/译文/源语言/目标语言/翻译状态**这五项（双语展示与用量对账的依据）；②§2.4 第 9 条与 P0-A-02 的**凭据 24 小时有效期、限流 30 次/秒**下沉，只保留「续期客服无感知」「禁用后 ≤5 分钟真断掉」「单客户端限流不波及同事」三条承诺；③P0-T-08 的**重试 5 次、指数退避、并发 20** 下沉，只保留「重试总时长 ≤40 秒」「连续 10 次失败熔断」「熔断不自动恢复」三条可见行为。§8 第 4 周新增产品参与压测结论确认——参数由技术定，但不得突破 PRD 承诺的上限 |
| v1.0-rc3 | **划清 PRD 与后端设计文档的边界**：rc2 写进来的租约机制（续租间隔、TTL 刷新算法、失败重试策略）属于实现细节，已从正文移出，改为只承诺两个**主管可见的上限**——卡死端口最迟 24 小时可用、服务端与客户端认知不一致最多 5 分钟；机制部分改为 `技术方案需回答` 交给后端设计文档。相应改写 P0-C-20 规则基线与 AC2/AC5/AC8/AC11/AC12、§6.3 决策 4、§8 第 2 周出口条件。同时补入两条**产品级**承诺（原本缺失）：**P0-A-01 AC2**——客服关机多久开机都不该重输密钥，凭据 24 小时有效期不得外溢成用户体验；**P0-A-02 AC12**——凭据轮换不得让任何一次业务请求失败，否则会喂给熔断计数。§7 增补 PRD 与设计文档的分工说明。全文复查同类越界：去掉「分片翻译」的实现表述（P0-T-07 AC10、P0-M-05 AC10、P0-B-10 AC12 改为「完整译文 / 只算一条」的结果表述）、去掉「不写死在代码里」（P0-S-12 AC12 改为「新增语种无需发版即可生效」）、去掉密码「加盐哈希」的算法指定（P0-A-19 AC16 改为「不得以可还原形式存储」） |
| v1.0-rc2 | Red-team 修订：①**端口租约参数落定**——TTL **24 小时** + 续租间隔 **5 分钟**（两者解耦：长 TTL 容忍异常、短间隔快速感知），新增 AC11 **手动释放端口**、AC12 **占用被撤销后客户端 5 分钟内感知**；②新增 **P0-A-01 AC15**——运行中服务端不可达时客户端**降级而不退出**、不得关闭浏览器实例（原设计下服务端抖动 2 分钟即全网停工），连带修订 P0-A-02 AC7 与 NG-18；③**引擎选择改为按语种能力路由**（P0-S-12 AC12），「语言不支持」不再是终态失败（P0-T-08 AC3、P0-T-06 AC6）——原设计下 Google-only 语种约半数消息会被随机命中的 DeepL 判死；④补齐**发出消息的原文回显与本地记录对齐**（P0-M-05 AC2/AC19/AC20），原设计中「客服侧同时展示原文与译文」缺实现路径且会产生两条本地记录；⑤R1/R5 标注为技术侧已提前验证，第 1 周未知收敛为**消息稳定标识**一项；⑥§4.2 补充 v1.0 实际内测规模（2 团队 ×5 人） |
| v1.0-rc | 后台账号定为**邮箱 + 密码、不做邮箱验证**；**平台管理员全系统 1 个、每团队主管 1 个**（取消新增管理员与多主管）；锁定策略 10 次/30 分钟、密码 ≥8 位含字母数字、不做找回密码；§2.5 发送方向源语言改为**自动识别**（客服可能直接输入英文）；修正 P0-C-20 AC8——租约被回收后账号须置「未启动」而非「离线」（离线仍占端口，会与服务端记录对不上） |
| v0.9 | 审核修订：**新增 P0-A-19 后台账号体系与登录**（原为空白）、**新增 P0-C-20 端口服务端裁决与租约回收**（原有端口永久泄漏漏洞）；新增 §2.5 语言口径，明确**客服工作语言固定中文**与双向翻译（原只定义了单向）；补充**消息标识稳定性**要求（去重与防重复计费的基础）；明确**熔断为客户端级**；补充**配额耗尽时接收方向的行为**；修正两项时延指标为客户端可观测的口径；明确**一密钥 = 一客服**；修正 P0-C-18 AC8 端口措辞与 P0-C-03 AC3 二维码措辞。精简：变更记录移入本附录、NG 表 26 → 23 条、§6.3 由 7 条降至 5 条、§6.5 去掉与排期重复的一列、删除重复的 AC 与附录行 |
| v0.8 | 删除「配额周期」概念，翻译配额改为累计总量，用量历史不按周期区分 |
| v0.7 | 去掉「续费」概念（只保留创建时刻与到期时刻）；展示时区固定 `Asia/Shanghai` |
| v0.6 | 配额周期改为按天数计算（v0.8 已废）；落入 OQ-44 / OQ-45 |
| v0.5 | 新增 P0-C-18 渠道账号环境配置（代理与指纹隔离）；时间点精确到秒 |
| v0.4 | 确立「注入式」客户端模型（重要修正）；统一翻译计量口径；删除 P1-M-14 |
| v0.3 | 落入 OQ-33 ~ OQ-40；删除 P0-C-04 与实例崩溃自动恢复；账号状态精简为 4 态；删除全部 P2 需求与容量成本推算 |
| v0.2 | 落入 OQ-01 ~ OQ-32 全部答复 |
| v0.1 | 首版，含 32 个待决问题 |

> 已作废且不复用的编号：`P0-C-04`（渠道账号掉线与恢复）、`P1-M-14`（原文/译文显示模式切换）。
